{
  "current": "1.108.278",
  "entries": [
    {
      "version": "1.108.278",
      "date": "2026-08-14",
      "title": "`exact` must mean exact, and a guardrail must not be its own baseline",
      "summary": "`_tokenize` folds case, strips leading underscores and drops punctuation, and the identity channel's tokenized comparison ran against that folded form. So for the query `_State`, a pytest fixture named `state` and the class literally named `_State` both scored `identity: 50.0, identity_type: \"exact\"`. The identity"
    },
    {
      "version": "1.108.277",
      "date": "2026-08-13",
      "title": "Reachability is not only the import graph, and liveness is not only the PID",
      "summary": "`.html` and `.htm` register on the bundled `html` grammar \u2014 the one `RAZOR_SPEC` already rides, so no new dependency \u2014 with **empty `symbol_node_types`**. An indexed template contributes **zero** entries to `index.symbols`, so every symbol-driven consumer (`find_dead_code`'s per-symbol sweep, the health-radar"
    },
    {
      "version": "1.108.276",
      "date": "2026-08-13",
      "title": "A Windows drive-root child can prove it is a repository",
      "summary": "On Windows, an explicit repository at `X:\\repo` has only two logical path components, so `index_folder` rejected it alongside genuinely broad paths. The guard now accepts that narrow case only when `.git` exists at the selected root. Drive roots, shallow non-Git directories, POSIX paths, and UNC depth handling are"
    }
  ]
}
