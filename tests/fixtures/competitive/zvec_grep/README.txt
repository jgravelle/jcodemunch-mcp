Captured 2026-09-07 from @zvec/zvec-grep 0.2.2 in the sandbox (sandbox/zvec_grep.Dockerfile) over the PINNED self corpus (this tree's src/ at 6e2c4b5a: 277 tracked files, of which the tool scanned and indexed 274; run.py --adapters zvec_grep --set none --runs 1, a smoke run, never recorded). Files are the container's /out as written; tools_list.json is from the second capture (the first failed: the token file is read, not created); the server log of that capture is not tracked (`*.log` is gitignored) and nothing reads it.
