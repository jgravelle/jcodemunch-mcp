# FINDINGS from the harness build (2026-09-03, branch `harness/source-of-truth`)

Rule: a test that fails against current code is a finding, never a weakened
assertion. Product-code seams are listed here too. IDs are referenced from
the tests that carry them.

| ID | Finding | Where | Status |
|---|---|---|---|
| F-01 | `SECURITY.md` limits table says "File count limit ... 500 files"; `security.DEFAULT_MAX_INDEX_FILES` is 10,000 and `DEFAULT_MAX_FOLDER_FILES` 2,000. | `tests/test_security_md_limits_parity.py::test_file_count_limit_row_matches_code` (strict xfail) | FIXED 2026-09-03: SECURITY.md row now states 10,000 / 2,000; the xfail marker is gone and both defaults must appear in the row |
| F-02 | `STANDARD.md` §4 stated the route floor as "route@1 >= 60% on the human corpus"; the gated assertion is control-subset route@1 >= 40.0 (baseline, minus 0.1) and 55.0 is the moratorium EXIT bar, a target. Design doc repeated the 55 as a floor. | `harness/thresholds.json` route.control_at1; `tests/test_catalog_moratorium.py` | FIXED 2026-09-25 (#715): item 7 and CONTRIBUTING.md's exit conditions now name `route.control_at1` and `EXIT_MAX_NAME_LEAKAGE`, and `tests/test_route_criterion_has_one_authority.py` scans every tracked Markdown file for a third copy. History: PARTLY FIXED, reopened 2026-09-16 as #715: the threshold file and §4's Floor line are correct, and **Definition of Regression item 7 still reads "Route@1 on the human corpus falls below 60%"** — a second normative site naming a different corpus AND a different number. The Phase 6 edit reached the reported paragraph only. Found by an external critique of 1.108.319 |
| F-03 | 12 config keys documented nowhere: `trusted_folders_whitelist_mode`, `server_output`, `server_output_threshold`, `worktree_base_path`, `git_root_identity`, `git_blame_enabled`, `summarizer_max_failures`, `cache_mode`, `summarize_from_docstrings`, `render_diagram_viewer_enabled`, `mermaid_viewer_path`, and `runtime_redact_enabled` (documented only as `JCODEMUNCH_RUNTIME_REDACT`). | `tests/test_config_docs_reverse_parity.py` INTERNAL_KEYS | OPEN: each needs a CONFIGURATION.md row or removal; the list may only shrink |
| F-04 | `tests/test_server.py` pins `len(tools) == 90` as a literal while the live `full` profile serves 91 (COVERAGE-MAP §4.3). The test passes because it disables `test_summarizer`; the literal is a second copy of the surface count. | `tests/test_server.py` | OPEN: LOAD-BEARING, untouched; should derive from `_build_tools_list()` |
| F-05 | Local skip count is 19 under `uv run pytest` and 13 under `PYTHONPATH=src python -m pytest` (CLAUDE.md). The delta is environmental (which optional deps the two interpreters see) and both are under the N7 floors (25 windows / 30 ubuntu). | `suite_before.log` vs CLAUDE.md | OPEN: reconcile which interpreter CLAUDE.md's line describes |
| F-06 | `benchmarks/cache_stability/results.json` is pinned by nothing and every number moved on re-run (reshuffled_share 0.139 -> 0.196). Excluded from every tier; UNCLEAR. | `harness/tiers.json` excluded/unclear | OPEN: pin a corpus snapshot or label non-deterministic |
| F-07 | Cold `search_text` was 8.8 s vs cold `search_symbols` 2.6 s on the full repo index (DISCOVERY §3). On the src-only self corpus the harness measures 251-359 ms cold and 135 ms warm p95 vs 10-13 ms for `search_symbols`: `search_text` is 10x the cost of `search_symbols` warm. | `harness/results/self_latency_three_runs.json` | OPEN: the first thing a latency profile should explain |
| F-08 | A 40 MB gitignored `.ab_bench_idx/` directory sits in the repo root and contains a recursively nested copy of itself three levels deep (an old A/B index that indexed its own store). Harmless to the harness; found by a grep for the attic scripts. | repo root, `.gitignore:55` | OPEN: delete by hand; not touched here |
| F-09 | The first draft of the self-latency harness repeated cold indexes in-process by wiping the store; the IndexStore LRU kept the old .db open and the second run failed `no such table: symbols`. Cold means a fresh process and a fresh store; the harness now does that. | `benchmarks/self_latency/measure.py` | FIXED in the harness (not a product defect) |
| F-10 | `benchmark.yml` has been warning "published benchmark numbers have moved" since 2026-08-31 (jcm tokens moved DOWN on all three repos). With `--floor` the upward direction fails; the downward direction still only warns and the artifact stays stale until re-synced. | `benchmarks/jcm_reference.json`, `results.md` | FIXED 2026-09-03: re-measured on the pinned corpora with `--verify-determinism --floor --reference` (identical across two processes; ratio 27.7x, per-repo rise max +1.5% on express, PASS); all five prose mirrors re-synced and `test_provenance.py` / `test_benchmark_reference.py` green |
| F-11 | The 0x08 scar recurred during this build: a `\b` inside a non-raw Python string in a heredoc became a literal BACKSPACE in `thresholds.json` guard patterns and the ratchet's non-vacuity test caught it (an inert pattern matched nothing). Same class as `tests/test_nesting_depth_channels.py`. | `harness/thresholds.json` history | FIXED; the non-vacuity check is what saw it |
| F-12 | Under a bare `python -m harness` (the conda interpreter on this box) pytest-xdist is not importable, the fast tier ran SERIAL at 108.9-110.4 s of pytest (110.95-121.05 s wall) and failed its own 90 s ceiling on all three runs. Under `uv run python -m harness` the same tier is 45.9 s of pytest (48.4 s wall). The ceiling was NOT loosened; the runner now probes the target interpreter and announces a serial fallback. | `harness/__main__.py::_xdist_args`; `scratchpad/harness_fast_run{1,2,3}.log` vs `harness_fast_uv1.log` | FIXED in the runner; the spelling in CLAUDE.md is `uv run python -m harness` |
| F-13 | The same pinned corpora give DIFFERENT jcm token totals on two machines: CI (ubuntu, run 33713310141) measured 23,440; this box measured 24,044 (express 5,085 / fastapi 11,092 / gin 7,867 vs CI 5,024 / 10,771 / 7,645). Each run is deterministic within its own machine (`--verify-determinism` passed both), so the benchmark is reproducible per box and NOT yet across boxes. The 2.5% gap is inside the 10% per-repo floor. Cause UNKNOWN (candidate: line-ending or path-length differences in the read source text). | `benchmarks/jcm_reference.json` (captured here) vs the CI log | CLOSED 2026-09-03 by a CI `--reference` run (benchmark.yml `reference=true`, run 33813819604) and a per-query diff against three local runs. THREE causes, all confirmed by removing them one at a time: (1) the local clones were CRLF (`core.autocrlf=true`), +603 tokens in fetched source; (2) tied ranking scores were broken by `os.walk` order, NTFS vs ext4 (gin `context bind`: five candidates at 10.202) -- fixed in `search_symbols` (the one product seam of this build) with `tests/test_search_symbols_tie_order.py`; (3) `_meta.total_tokens_saved` is read from HOME, so its digit width differs per box (+1 search / +3 fetch per query; F-17). With LF, a fresh store and the tie fix, `stable_tokens` are IDENTICAL on all 15 queries between this box and CI; the published figure differs only by the ledger width. The CI artifact is now the committed reference (23,467; 28.3x) |
| F-14 | Every test that counts tokens (schema budget, benchmark harness, negative evidence: 4 failed + 22 errors in the fast tier) failed on the FIRST CI run of `harness.yml` because tiktoken fetches its `cl100k_base` BPE asset on first use and `conftest._no_network` refused it. Invisible on this box, which has the asset cached. Reproduced locally with an empty `TIKTOKEN_CACHE_DIR`. | `harness/__main__.py::warm_assets`, `python -m harness warm`, `test.yml` | FIXED 2026-09-03: the runner warms the asset OUTSIDE the pytest session before the fast and full tiers; `test.yml` runs `harness warm` before pytest; the fixture's message names the command. No test was marked `network` |
| F-15 | The bench tier's `replay_gate` ran `run_replay.py` against the fixture's repo id, which is a hash of THIS box's index path, so on CI it found no index and scored mrr 0.0 against a golden 1.0 (a missing index reported as a total regression). `replay.yml` had solved this with `--repo`/`--storage-path`; the harness step did not. | `harness/tiers.json` replay_gate `self_index`, `harness/__main__.py::self_index` | FIXED 2026-09-03: the runner indexes the repo into a fresh temp store per run and passes the measured id; the store is deleted after. A relative path made `index_folder` log a warning into the captured stream, so the id is read from a `REPO_ID=` sentinel |
| F-16 | All nine in-tree corpus checksums MISMATCHED on the first CI run after F-14/F-15: the manifest was pinned on this box, where `core.autocrlf=true` checks the files out CRLF, and CI checks out LF. Every pinned digest covered line endings, not text. | `harness/corpora.py::_digest`, `harness/corpora.json` | FIXED 2026-09-03 in two steps: the digest folds CRLF to LF before hashing (eight of nine then matched CI), and files are sorted by their POSIX relative string in byte order, because `sorted(Path)` is case-insensitive on Windows and put `REGENERATE.md` last here and first on the runner (the ninth). Every re-pinned value equals the one CI printed on its LF checkout (e.g. `replay_golden` 4abedeef27fb, `racket_fixtures` 1f070fa58a51) |
| F-17 | The token benchmark's published count embeds the developer box's lifetime savings ledger: `_meta.total_tokens_saved` is read from `~/.code-index/_savings.json` in HOME (`token_tracker._savings_path(None)`), NOT from `CODE_INDEX_PATH`, so a fresh store does not isolate it, every benchmark run grows the maintainer's real ledger (8,994,101 -> 9,853,917 during this investigation), and its digit width is +1 search / +3 fetch tokens per query against a fresh runner. Two questions for jjg: (a) should the ledger honour `CODE_INDEX_PATH` (a product change that relocates the counter for anyone who sets it, so their lifetime total appears to reset); (b) should the published basis pin the volatile `_meta` keys, or drop `_meta` entirely, since `meta_fields: []` is the shipped default and an agent on a default install receives no `_meta` at all (the "payload as the agent receives it" argument in `run_benchmark.py` is wrong on the default config). Either is a basis change (SAVINGS_BASIS_GENERATION shape) and a CHANGELOG event. | `benchmarks/harness/run_benchmark.py::measure_jmunch`, `storage/token_tracker.py:1386` | OPEN: documented in REPRODUCING.md; the reference is captured on CI where the ledger is fresh, so it is reproducible there |
| F-18 | `uv run --python 3.10 pytest ...` (used once to verify the pre-flight script) silently rebuilt `.venv` on 3.10 WITHOUT the watch extra; the next fast tier passed at 1055 passed / 112 skipped, exit 0. The full tier has a skip ceiling; the fast tier had none. | `harness/thresholds.json` suite.fast_skips_max (15; observed 7 local / 9 CI) | FIXED 2026-09-03: fast-tier skip ceiling; repair is `uv sync --locked --group dev --extra watch --python 3.12` |
| F-19 | The self-latency Floors were set at 2x the median of three runs on THIS box, and the bench tier on CI failed one once: `latency.get_symbol_source_warm_p95_ms` observed 72.7 ms against a 27 ms floor (run on e8fd4f4); the next run on 9b2123d passed. Runner noise is UNMEASURED (VERIFICATION §1 said so). The tolerance rule is that a timing floor tightens or moves only after CI has its own three runs. | `harness/thresholds.json` latency.*, `harness/results/self_latency_three_runs.json` | OPEN: collect three CI `self_latency.json` artifacts (harness.yml uploads them) and set the CI floor from their median with a `loosened` block if it must move; do NOT loosen from one flake. ⚠⚠ **SECOND OCCURRENCE 2026-09-12, and the sentence that used to end this row is now FALSE.** On PR #669 (run 34720102614, job 103625872324) `latency.get_file_outline_warm_p95_ms` observed **64.9 ms against a 28 ms floor**, `HARNESS FAIL`, on a diff of one docstring and two docs files. A different metric from the first occurrence, the same shape: one latency Floor fails while every sibling passes with room on the same run (`search_symbols` 5.6/23, `get_symbol_source` 1.1/27, `search_text` 60.0/271, both `index.*` well under). Two CI failures on two different `latency.*` ids now, both on changes that cannot touch latency, which is evidence about the RUNNER and not the tree — and still only two of the three runs the tolerance rule requires before the floor may move. ⚠⚠ **This row previously ended "The bench tier is not a required check, so this cannot block a merge." `bench: harness bench tier` IS a required context on `main` (RUNBOOK §8's `gh api` call lists it), and it blocked #669.** A status line that says a failure is harmless is worse than no status line; same class as F-27, and found the same way — by the claim being acted on. ⚠⚠ **THIRD OCCURRENCE 2026-09-13, and the first with TWO ids failing on one run.** On PR #677 (run 34764754247, job 103745491651) `latency.get_symbol_source_warm_p95_ms` observed **110.0 ms against 27** and `latency.get_file_outline_warm_p95_ms` **93.1 ms against 28**, while `search_symbols` read 6.9/23 and `search_text` 86.9/271; the diff was the stamp hook, one test and docs. **The same job re-run (103757575071) read 1.6 and 1.8**, and `get_file_outline_warm_median_ms` was 1.2 on the failing job and 1.3 on the re-run — so the median did not move and the p95 did by 50x. That is a TAIL on the runner, not a slower tool, and it is the shape a p95 over a short sample on a shared runner produces. Still zero of the three clean CI `self_latency.json` artifacts the tolerance rule asks for have been collected; three failures are not that evidence and do not license moving a floor. Each one costs a diagnose-and-re-run cycle on a required context. ⚠⚠ **FOURTH OCCURRENCE 2026-09-13, and the first on `main`** (#678, opened by `main.yml`, closed by a person). Run 34770211726 on 95ee515d, job 103758319238 `main: harness bench (online)`: `latency.get_file_outline_warm_p95_ms` observed **89.0 ms against 28** while `search_symbols` read 4.7/23, `get_symbol_source` 16.8/27 and `search_text` 70.5/271, and `get_file_outline_warm_median_ms` read **1.0**: the median did not move and the p95 did, the third occurrence's shape again. The range 6774ed4e..95ee515d does not touch the outline path, and the next three `main.yml` runs (abcba781, b9dfcb19, b2869b52) passed. Four failures on two ids, all on changes that cannot move latency; still zero clean CI `self_latency.json` artifacts collected for the tolerance rule. ⚠⚠ **FIFTH OCCURRENCE 2026-09-16, on the 1.108.319 release PR (#704), and the diff contains NO CODE AT ALL** — version pins, `CHANGELOG.md`, `CLAUDE.md`, `ISSUE-HISTORY.md` and one `FINDINGS.md` row. Run 35087276319, job 104769127920 `bench: harness bench tier`: `latency.get_symbol_source_warm_p95_ms` observed **109.8 ms against 27**, while `search_symbols` read 8.2/23, `search_text` 143.6/271 and `get_file_outline` 1.5/28 — one id over, every sibling passing with room, the third and fourth occurrences' shape exactly. The same job re-run (104774625932) passed in 53 s. It blocked a required context on a release PR and cost a diagnose-and-re-run cycle, which is now five for five. **Still ZERO of the three clean CI `self_latency.json` artifacts the tolerance rule asks for have been collected, and five failures are not that evidence.** ⚠ Five occurrences on two ids, every one on a change that cannot move latency, is an argument that the CI floor was set from the wrong box — **which is a reason to collect the three artifacts, not a reason to loosen from a flake.** ⚠⚠ **SIXTH OCCURRENCE 2026-09-19, the second on `main`** (#791, opened by `main.yml`, closed by a person). Run 35449592648 over 3d386d50..ea9c16b0: `latency.get_file_outline_warm_p95_ms` observed **32.5 ms against 28** with a median of **1.0**, every sibling latency Floor passing. The range's only code change (#773) is in `check_delete_safe`, `get_group_contracts` and one function appended to `runtime/confidence.py`, none of it on the outline path. The next `main` run, 35450285331 on 51dea69d, contains that commit and read the same Floor at **1.9**. The smallest overshoot of the six, and the same shape: the median still and the p95 moved. Floor unchanged; still zero clean CI `self_latency.json` artifacts collected. ⚠⚠ **SEVENTH OCCURRENCE 2026-09-19, hours after the sixth**, on PR #793 (run 35458719607, job 105940352055 `bench: harness bench tier`): `latency.get_symbol_source_warm_p95_ms` observed **50.8 ms against 27** while `search_symbols` read 7.2/23, `search_text` 124.3/271 and `get_file_outline` 12.9/28 with a median of 1.2. The PR filters discards inside `parse_file` and does not touch the read path. The failed job was re-run and passed. Seven for seven on changes that cannot move latency. ⚠⚠ **EIGHTH OCCURRENCE 2026-09-21, and the FIRST on a THIRD id.** On PR #815 (run 35591019038, job 106308704493 `bench: harness bench tier`) `latency.search_symbols_warm_p95_ms` observed **54.7 ms against 23** while `search_text` read 39.6/271, `get_symbol_source` 1.0/27 and `get_file_outline` 0.9/28 with a median of **0.7** -- one id over, every sibling passing with room, the established shape. **Until now every occurrence was `get_symbol_source` or `get_file_outline`; this row can no longer be read as two unlucky ids.** ⚠⚠ The diff is a PARSER change (four custom extractors gaining class-state branches), so unlike the previous seven it is not self-evidently unable to move latency -- and the discriminator is that the two INDEXING Floors, the ones extraction actually moves, passed with the largest margins on the run: `index.cold_self_seconds` 1.86/5.2 and `index.one_file_reindex_ms` 46.3/284. `search_symbols` is a READ over a built index and the same commit measured 8.2/23 locally. **Naming the mechanism that WOULD have shown a real regression, and checking it, is what separates this from waving a flake through.** Job re-run rather than the floor loosened. Still zero clean CI `self_latency.json` artifacts collected. ⚠⚠ **NINTH AND TENTH OCCURRENCES 2026-09-23, within 21 minutes of each other, on the same id.** PR #814 (run 35800049585, job 106991240506 `bench: harness bench tier`, the weekly bench-results bot PR, docs only) `latency.get_symbol_source_warm_p95_ms` observed **65.8 ms against 27**; PR #831 (run 35801692026, job 106995868957, the C struct-member fix, no read-path change) the same id at **106.0 ms against 27**. Both jobs re-run and passed. ⚠⚠ **The three artifacts this row has asked for since 2026-09-03 were never absent -- they were uploaded on every green run and nobody downloaded one.** `pr-gate.yml` uploads `harness-bench-<sha>` and `main.yml` uploads `harness-main-<sha>`, each carrying `self_latency.json`. SIX from 2026-09-22 alone are now in `harness/results/self_latency_ci_runs_2026-09-22.json`, verbatim, with the run ids. What they say, read off that file: on a clean runner `get_symbol_source_warm_p95_ms` reads **1.2-1.8 (median 1.5) ms** against the 27 floor, `get_file_outline` **1.3-13.1 (median 1.9)** against 28, `search_symbols` **5.1-11.6 (median 7.5)** against 23, `search_text` **50.6-86.2 (median 64.0)** against 271. ⚠⚠ **So the failures are 34x to 73x the clean median on `get_symbol_source` (six of the ten: 72.7, 110.0, 109.8, 50.8, 65.8, 106.0 against 1.5), 18x to 50x on `get_file_outline` (64.9, 93.1, 89.0, 32.5 against 1.85) and 7.3x on `search_symbols` (the one occurrence, 54.7 against 7.45), with the median unmoved every time, and NO FLOOR LEVEL COVERS THAT: a floor above 110 ms would catch nothing on the two `get_*` ids, and 27 is already 15x the largest clean `get_symbol_source` p95 (1.8) and 18x its median.** The Floor is not set from the wrong box; a p95 over 20 calls on a shared runner is the wrong instrument for a gate, because one preempted call is the 95th percentile of twenty. The candidate is in the MEASUREMENT, not the number: re-sample once on a FAIL within the same job before reporting it (a tail that reproduces is a regression; one that does not is the runner), or gate the median and REPORT the p95. Either is a harness design change with its own PR; the floors stay where they are. ⚠ Also visible in the six files and unexplained: three distinct `corpus_sha256` values across runs of one pinned corpus, which F-34's MISMATCH check would refuse locally and which the bench tier on CI evidently does not.|
| F-20 | `thresholds.py` announces a LOOSENING only `if hist:` — an entry whose `history` is empty (all 33 today) can have its `floor` moved the wrong way and load silently; `guard_patterns` and `set_at.reason` keep the old literal, so `tests/test_thresholds_are_the_only_copy.py` guards a value the file no longer states. Found 2026-09-04 by the workflow layer's reviewer subagent on a probe diff (`coverage.min` 74 -> 64, no `loosened` block): both fresh-context runs named it (docs/workflows/VERIFICATION.md section 3). | `harness/thresholds.py`, `harness/thresholds.json` | OPEN: compare against the file's own `set_at`/previous value, not only `history`; the reviewer's DoD-12 diff check and `dod_checklist.py` row 12 are the interim gates |
| F-21 | `tests/test_result_cache_isolation.py` (LOAD-BEARING, the #572 guard) does `from ... import _isolate`, so when the workflow layer's `/fix-issue` probe reintroduced #572 by reverting the fix, the guard failed as ONE collection `ImportError` and a plain `pytest` run ABORTED before any assertion ran (`Interrupted: 1 error during collection`, exit 2). The row's promised failures collapsed to an import error, and only `--continue-on-collection-errors` let the reproduction run. Found 2026-09-04 (docs/workflows/FINDINGS.md W-32). | `tests/test_result_cache_isolation.py`, ARCHAEOLOGY row 315 | OPEN: a guard for a behaviour should reach it through the public entry points (`cache_put`/`cache_get`), keeping the helper import in a separate test whose failure does not abort collection |
| F-22 | `tests/test_v1_108_172.py::test_active_use_keeps_an_entry_alive` failed once on `full: test (windows-latest, 3.13)` for PR #593, a docs-only change (`assert None is not None`; 1 failed, 9329 passed). The test sets a 0.3 s idle TTL on the index cache and touches the entry every 0.1 s, so its margin is 0.2 s of REAL time under xdist on a shared runner; one gap ran past the TTL and the entry was evicted. The other seven matrix jobs and the re-run passed. First time-based flake seen on the gate since the harness shipped. | `tests/test_v1_108_172.py` (ARCHAEOLOGY row for `test_v1_108_172.py`), `src/jcodemunch_mcp/storage/sqlite_store.py` TTL cache | FIXED 2026-09-04: the four TTL tests inject a clock (`_Clock`, forwarding everything but `monotonic` to the real module) and advance it by hand, 10 s TTLs, no sleeps; deleting the refresh-on-hit line in `_cache_get` still turns `test_active_use_keeps_an_entry_alive` red |
| F-23 | The bench tier's `self_latency` step wrote its result to the TRACKED `harness/results/self_latency.json` on every run, on any branch, with or without `--write-results` (`tiers.json` passed the tracked path as `--out`; `latest.json` was already scratch-first). A bench run on the `competitive/cf61-reindex-one` branch (2026-09-09) left the file dirty in the working tree (its `date` and `commit` fields moved), and the NEXT PR's checklist, a test-only diff, read `harness/` as changed and demanded a bench tier (DoD 10 unmet) until the file was restored by hand; the full-tier stamp then moved with the restore, and `pre_pr` refused the PR until the tier ran again. W-29 had closed the compare command's half of this (it restores both files by name) and not the runner's. | `harness/__main__.py::tier_bench`, `harness/tiers.json` (`self_latency`) | FIXED 2026-09-09: `tier_bench(write_results=)`, the `artifact` key on the `self_latency` step, `tests/test_harness_bench_artifact.py` (a run leaves the tracked path alone and still reads the artifact; `--write-results` copies it; a failed step copies nothing; the shipped step declares the path its `--out` names). `pr-gate.yml`, `main.yml` and `nightly.yml` pass `--write-results`, so the CI artifact uploads and the weekly results PR still carry the fresh file. A failed step now keeps its `{rc, seconds, tail}` record in `result["artifacts"]` where the runner used to replace it after the loop with whatever stale tracked file existed (the F-23 reviewer, note 2). The shape: a step declares its `artifact`; the runner substitutes a scratch path for it in the command, reads the artifact from scratch into `result["artifacts"]`, and copies it into the tree only under `--write-results`; the Floor verdicts are unaffected because `measure.py` prints them from memory, not from the file |
| F-24 | The bench summary carries 2 of the 6 `self_latency` Floors (`bench.md` shows `latency.get_symbol_source_warm_p95_ms` and `latency.get_file_outline_warm_p95_ms`; `tiers.json` declares six on the step) because `tier_bench` re-prints only the last three lines of a step's stdout into the verdict tee, and `measure.py` prints its six verdict lines last. The tier's exit code still carries a FAIL (`measure.py` exits non-zero on one), so a Floor violation is not lost; the summary table a reviewer reads under DoD 10 is. Found by the F-23 reviewer (2026-09-09). | `harness/__main__.py::tier_bench` (the `tail` of a step), `benchmarks/self_latency/measure.py` | FIXED 2026-09-09: `tier_bench` re-prints every `_VERDICT_RE` line of a step's stdout, then the last three non-verdict lines, so no verdict prints twice; the record keeps the raw three-line tail for the log. `tests/test_harness_verdict_lines.py` (a six-verdict step reaches the tee with all six, once; the shipped step's declaration is the six ids). Red against main: 1 failed, the tee saw one of six |
| F-25 | `tests/test_competitive_reindex_one.py::test_our_adapter_reports_the_workers_measurement` failed `adapter jcodemunch: pin must be a Pin` on five of the eight `full: test` jobs for PR #651 on the gate's first attempt (ubuntu 3.11 and 3.13, windows 3.10, 3.11 and 3.12) and on three of those five when they were re-run (ubuntu 3.13, windows 3.11 and 3.12; `evidence/gate_651.txt`, from the run's job list), a test-only diff, and passed on the rest. The three competitive module fixtures (`test_competitive_reindex_one.py`, `test_competitive_fairness_digest.py`, `test_competitive_image_cost.py`) popped `adapter`, `run` and friends from `sys.modules` before importing them, so under any xdist ordering where a file that imports `adapters.jcodemunch` (`test_competitive_sandbox.py`) ran first in the same worker, that adapter held the FIRST `adapter.Pin` class while `validate` compared against the SECOND; `isinstance` failed. Reproduced in one process on unmodified main by running the sandbox file before it (`evidence/red_identity_main.txt`: 1 failed, 19 passed). Second ordering flake on the gate after F-22. | the three fixtures | FIXED 2026-09-09, by importing without popping so the module cached by an earlier file is the one every `adapters.*` module already holds: the three fixtures import through `importlib.import_module` with nothing popped; the same order passes (49 passed, one process). `tests/test_competitive_reindex_one.py`, whose `test_no_competitive_test_pops_the_tier_modules` scans every `tests/test_competitive_*.py` for `sys.modules.pop` and fails on one, so a reintroduced pop is caught by the scan and not by xdist ordering (review round 1); and `test_the_adapters_hold_the_same_pin_class_the_fixture_imported` asserts the property itself, `adapters.jcodemunch`'s `Pin` is `adapter.Pin`, which a re-import by any spelling (`importlib.reload`, `sys.modules.clear()`) breaks where the scan sees two spellings (the #652 reviewer's note); the adapter is bound at COLLECTION time in that module, so the assertion, and the gate's own test with it, go red on a reintroduced pop under every ordering, the file alone included (3 failed alone, 3 failed with the sandbox file first), where before only the ordering with the sandbox file first could fail |
| F-26 | `tests/test_watcher_symlink_safety.py::test_removing_watched_root_fails[polling]` failed once in the full tier on this box (2026-09-12, branch `feat/compiler-diagnostics`, commit b4ecd2ba, tree `b7e81259b1d4`; the run's summary is kept as `.claude/state/evidence/full_first_run_failed.txt` on that branch: `1 failed, 10252 passed, 24 skipped ... in 297.56s`), on a diff that touches no watcher code, after three fast tiers and a full tier had run back to back. Re-run alone it passed, and the full tier on the branch's final commit passed (`full.md`: `10256 passed, 24 skipped ... in 354.84s`). `run_full.py` keeps only pytest's summary, so the assertion text is not in the evidence. Same family as the windows 3.11 registration-race flake (run 34644461374, 2026-09-11, a different test in the same file); recorded rather than re-run silently, per that entry's rule that a second watcher flake is a FINDINGS row. Two more facts from the same day, both about the box and not the tree (session observations from the task notifications and a `Get-Process` listing, no evidence file carries them): two full-tier runs were killed for low memory while DaVinci Resolve held 3.3 GB beside `-n auto`'s workers, and passed at `PYTEST_XDIST_AUTO_NUM_WORKERS=6` (356.7 s against the 360 s Floor, so the cap costs most of the margin); and a run whose tree changed underneath it (`tree changed during the run; stamp invalid`) while a reviewer subagent ran targeted pytest in the same checkout. | `tests/test_watcher_symlink_safety.py` (polling arm), `.claude/hooks/run_full.py` (keeps no traceback; no worker cap) | OPEN 2026-09-12. Next: make `run_full.py` keep the `FAILURES` section of a red run beside `full.md`, so the next flake carries its assertion, and accept a worker cap so a loaded box does not kill the run; if the polling arm fails again, pin the interleaving ([[a-concurrency-test-must-pin-the-interleaving]]) rather than re-run |
| F-27 | **A `thresholds.json` entry carries a `target` beside its `floor` and ONLY the floor is gated, so every target is a number nobody enforces.** Two measured 2026-09-12, both already breached with the suite green: `schema.core_compact_ceiling` floor 4,000 / target 3,800, observed **3,967**; and `claude_md.max_chars` floor 140,000 / target 120,000, observed **139,978** — the target missed by ~20k while the floor is clear by **22 characters**, on the one entry whose `loosened` block reads "the 10k buffer is the last one. Never raise again." ⚠ The second is the live landmine and it was found by accident: CLAUDE.md's own core_compact entry had transcribed the headroom as "3,998, TWO tokens" (2026-09-02, #571) and was 31 tokens stale within ten days, so the recorded literal overstated the danger on one Floor while the real one sat unrecorded on another. De-transcribed the same day; the number now lives only in `benchmarks/schema_baseline.json`. | `harness/thresholds.json` (`target` on every entry), `harness/thresholds.py`, `tests/test_claude_md_size.py`, `tests/test_schema_budget.py` | OPEN 2026-09-12, raised by jjg for whoever owns the harness work: a `target` either earns a WARNING-level verdict (announced every run, never failing) or is dropped from the schema as decoration. ⚠⚠ Do NOT promote a target to a floor — `claude_md.max_chars` goes red immediately. ⚠ CLAUDE.md sits 22 chars under its floor right now, so any addition anywhere needs a matching deletion and a rotation is still two edits |
| F-28 | `tests/test_watcher_registration_indexing.py::test_registration_race_updates_persisted_symbols[live_tree-polling]` failed on `full: test (windows-latest, 3.12)` for PR #669 (run 34717936085, job 103618584219): `1 failed, 10255 passed, 24 skipped ... in 814.59s`. **Second occurrence of THIS test** — the first was windows 3.11 on 2026-09-11 (run 34644461374), recorded then with the rule that a second is a FINDINGS row rather than another re-run. The diff is a docstring and two docs files, nothing within reach of the watcher; the other seven matrix jobs passed, and the full tier passed twice locally on the same tree (`9df4f8b00e61`, 10256 passed / 24 skipped, 298.72 s and 327.89 s). ⚠ Unlike F-26 the assertion SURVIVED, because CI keeps it where `run_full.py` does not: `AssertionError: Timed out waiting for ('after_recovery', 'code.py') present=True; persisted=[('during_arm', 'code.py'), ('hidden_before', '.github/hook.py'), ('nested_symbol', 'child/nested.py')]; arms=[['.', '\\.github', '\\child']]`. So the polling watcher observed the arm-time write and the two pre-existing files and never observed the post-recovery one: the recovery re-registration and the write raced, and the write landed in the window where no arm covered it. Third watcher flake in three days (F-22 TTL, F-25 xdist identity, F-26 symlink polling), and the second on the polling arm specifically. | `tests/test_watcher_registration_indexing.py` (polling arm), `src/jcodemunch_mcp/watcher.py` re-registration path | OPEN 2026-09-12. Per F-26's rule this is recorded, not re-run silently. Next: **pin the interleaving** ([[a-concurrency-test-must-pin-the-interleaving]]) — the polling arm's recovery step needs a deterministic barrier between re-registration and the write rather than a timeout, and the non-vacuity tell is that the pinned version must fail against a watcher that re-registers late. ⚠ Do NOT raise the timeout: F-22's fix was an injected clock, not a longer wait, and a timeout raise here would hide the race the test exists to find |
| F-29 | `tests/test_inbound_workflows.py::test_executed_gates_start_a_model_job_only_with_both_switches_on[inbound-triage]` failed the nightly matrix on **two different Python versions on two consecutive nights** with the same error: `subprocess.TimeoutExpired ... timed out after 60 seconds`, on the `classify-queue-go-True-True` combination both times. Run 34928169500 (2026-09-15, `full tier (windows-latest, 3.10)`, on `6d1258e`) and run 35054927779 (2026-09-16, `full tier (windows-latest, 3.13)`, on `139f85c3`). The other nine matrix jobs passed each night and `main.yml` was green on both commits. ⚠⚠ **THE CAUSE IS NOT SLOWNESS AND THE FIRST WRITE-UP OF THIS ROW WAS WRONG ABOUT IT** — it blamed 16 process launches under contention, which is what the evidence looked like before anyone ran `command -v` inside the harness. The harness stubbed `gh` and `python` as EXTENSIONLESS FILES in a `bin/` directory prepended to `PATH`, with `chmod +x`. **Git Bash on Windows resolves a command only through an executable EXTENSION, so the executable bit is irrelevant and the stubs shadowed NOTHING**: `command -v gh` returned `C:\Program Files\GitHub CLI\gh` and `command -v python` the venv interpreter. Every Windows run of this test therefore called the REAL `gh` against the fake repo `o/r`, over the network, with whatever credentials the runner had — **a test reaching real `gh`, which this project forbids outright** — and a real `gh issue list` on a contended runner with no token does DNS, TLS and auth resolution before giving up. That is the 60 seconds, and it is why both failures name `True-True`: it is the ONLY combination that reaches the `gh` branch. ⚠⚠ **It never went red, because the gate writes `gh ... | tr ... | sed ...` and a pipeline reports its LAST command's status** — the real `gh` failed, `sed` succeeded, `go=true` was written anyway, and the check passed over a call it should never have made. Second instance of inbound item 6's lesson (09-04, a gate's exit status is never the left side of a pipe), this time inside the FIXTURE that checks it. ⚠ Linux was unaffected and green throughout, which is why five months of CI never saw it: an extensionless stub on `PATH` works there. | `tests/test_inbound_workflows.py` `_STUB_PRELUDE` / `_run_step` / `_gate_outputs` | FIXED 2026-09-16 (#705): the stubs are SHELL FUNCTIONS, which bash resolves ahead of `PATH` on both platforms and which need no file, no `chmod` and no process. `test_a_gate_step_cannot_reach_the_real_tool[gh|python]` asserts the RESOLUTION (`command -v` must answer the bare name), because resolution is the property a file on `PATH` gets right on one platform and wrong on the other; non-vacuity was run as the new assertion against the OLD mechanism, which reported the real `gh` and the venv interpreter. `test_the_stub_refuses_a_command_it_does_not_implement` is the over-suppression guard — dispatch is by basename and an unrecognised script exits 127 with a named message, so a gate step that starts calling a fourth script fails loudly instead of being silently answered (#569's lesson). Measured back to back in one session and recorded in `.claude/state/evidence/timing.md`: **2 failed, 156 passed in 18.71s** before, **158 passed in 4.21s** after — the two failures being the new guards reporting the resolution, and the rest passing because a fast local `gh` behind a status-hiding pipe is what made this invisible. A runner without a usable `gh` is where the 60 s came from and this box cannot reproduce it. ⚠ **The `stdin=DEVNULL` item the first version of this row named is MOOT, not outstanding**: with the stubs as functions a gate step spawns no child process for them, so there is no inherited stdin left to block on. Retired here rather than dropped silently. ⚠ The `timeout=60` is KEPT as a hang guard and is no longer a budget — nothing in a step can legitimately take seconds now. |
| F-30 | **The token benchmark gates COST and nothing gates SUCCESS on the same tasks, so a retrieval that missed the answer scores as an efficiency gain.** `benchmarks/METHODOLOGY.md:10` excludes answer quality and task completion by design, and that exclusion is disclosed; what is not established is the step from there to "token reduction per task" in outward-bound prose, or to `STANDARD.md`'s Definition of Regression rule that a move toward fewer tokens "is not a regression". The parser defects #712-#714 make it concrete: a named JavaScript generator has no retrievable symbol, so answering its question returns less. ⚠ NOT measured — the benchmark was not re-run with a deliberately damaged parser, so no gain from these defects is asserted. | `benchmarks/METHODOLOGY.md`; `docs/standard/STANDARD.md` Definition of Regression | OPEN: condition a token improvement on unchanged success and evidence coverage over the same cases; the damage-injection run is what would settle whether the current gates can see the trade. Raised by an external critique of 1.108.319 |
| F-31 | `tests/test_inbound_scan.py::test_the_security_scan_is_linear_on_adversarial_input[leak+spaces]` failed the full tier on this box (2026-09-16, branch `fix/711-journal-absence-is-repo-scoped`, `1 failed, 10564 passed, 24 skipped in 244.47s`). The assertion is a WALL-CLOCK RATIO -- `t_large <= 10 * max(t_small, 0.005)` over `_best_of_three` -- so it measures the scan AND whatever else the box was doing, and `-n auto` runs it beside ~10.5k other tests. Re-runs pass: all 20 parameters in 14.71s, `[leak+spaces]` alone in 0.96s. ⚠ Every number here is read off `.claude/state/evidence/full_f31_failed_run.txt`, which retains the failing tier's own output and the captured re-runs; an earlier draft of this row quoted 16.19s/12.29s from an uncaptured run and the isolated case actually measures 0.96s. ⚠⚠ The tier log records only the test id, **not the observed `(shape, t_small, t_large)` tuple**, so the margin it missed by is unknown and no number is quoted for it. ⚠ F-30 is taken by a row on another branch (the docs PR); this is F-31 to avoid two different F-30s meeting on main. | `tests/test_inbound_scan.py:392-401` | OPEN: fourth timing-sensitive test to flake under the parallel full tier (F-19, F-26, F-28, F-29) and the first outside the watcher/latency families. One guard measuring a PROPERTY -- no square law -- through an instrument that cannot separate it from scheduler noise. **Do NOT loosen the 10x**: it is 2.5x the linear expectation against a 16x square law, so widening it admits the defect. The candidate fix is to make the assertion carry its evidence into the tier log, and only then decide whether the instrument or the code is at fault |
| F-32 | **Coverage's C tracer was ~40% of the full tier's wall clock, and a single CI run crossing `suite.full_seconds` is what surfaced it (#740).** Controlled, back to back on the measuring box, same tree, same command, `-n auto`: ctrace 336.33 s and 332.41 s (mean 334.37), sysmon 203.98 s, uninstrumented 199.16 s -- so sysmon's instrumentation costs 4.82 s against ctrace's 135.21 s, a 39.0% reduction in tier wall time. ⚠⚠ **The coverage number is NOT weakened, and the control is what shows it:** ctrace reported 9475 then 9471 missed lines on two runs of the SAME core (-4), while sysmon reported 9470 (-1 against ctrace's second run) -- the between-run difference inside one core is larger than the between-core difference, and both deltas live entirely in `server.py` (1132 -> 1127 -> 1127), the async dispatcher. Statements identical at 52487 across all three instrumented runs. ⚠ Not version-gated by us: coverage checks sys.monitoring, branch support, dynamic contexts and concurrency, then warns (`no-sysmon`) and falls back, so the 3.10/3.11 matrix legs are unchanged. | `harness/__main__.py::tier_full`, `tests/test_full_tier_coverage_core.py` | FIXED 2026-09-17. No Floor moved: DESIGN's `2x median` tolerance rule would yield 395 from the three runs at this commit, LOOSER than the 360 in force, so it is recorded and declined. Numbers written from `.claude/state/evidence/740_measurement.json`, never typed |
| F-33 | **`suite.fast_seconds` cannot tell a slow tree from a busy box, and the busy box was diagnosed wrong twice in one afternoon.** On 2026-09-19 the fast tier read 118.91, 97.3, 108.3, 156.93, 148.27, 100.73 and 144.91 s against the 90 s Floor on one branch, `origin/main` read 105.42 s on the same box, and the full tier climbed 206.88, 275.67 and 306.89 s. The cause was another Claude session's ONNX benchmark (`miniconda3` python, 54 threads): **20.0 CPU-seconds per 5 s sample, 7,338 CPU-seconds in about ten minutes on a 24-core box**. It was first called heat (ten minutes idle, then 108.3 s) and then a power-mode problem (performance mode, then 144.91 s). Every point-in-time probe read healthy: CPU load 4%, clock at maximum, disk queue 0.04, 14.7 GB free. With the job gone the same tier read 67.26 and 66.63 s. ⚠⚠ **The commit hook's 150 s budget makes this worse: past it the fast tier is SKIPPED with a warning and the commit goes through**, which happened twice (`54cb432a`, `90515c1d`). Both were caught because the warning was read, and an explicit run then said FAIL. | `.claude/hooks/pre_commit.py` (the budget), `harness/thresholds.json` `suite.fast_seconds` | OPEN, and the Floor does not move. Two candidates: the hook REFUSES past its budget where it skips today, since a tier too slow to finish has not passed; and the harness prints the top CPU consumers that are not its own children when `suite.fast_seconds` fails, so the first reading names the contender. The procedure until then is a 5 s CPU delta over python processes the session did not start, BEFORE any idle wait or A/B. ⚠⚠ **FOURTH CAUSE, 2026-09-20, and it is PHYSICAL: the fans were obstructed.** ⚠⚠ **Provenance first, because this row nearly repeated F-31's mistake one row above: three of the four fast-tier readings below were transcribed from the commit hook's refusal text, NOT read off a retained artifact** -- `evidence/fast.md` is rewritten by every tier run, and the cause was physical and has been cleared, so none of the pre-clearing readings can be reproduced. ⚠ **The attribution is INLINE here on purpose: `.claude/state/` is gitignored (`.gitignore:65`), so a row citing an evidence path sends every other reader to a file that does not exist on their checkout** -- which F-31 and F-32 above both do, and which is worth fixing when either is next touched. Provenance, then, and it is the SAME for every number in this row: **all of them are transcribed from the commit hook and harness stdout as they appeared, and none can be read back.** A first draft of this sentence attributed two of them to `evidence/fast.md` and `evidence/full.md` -- which was false within the hour, because those files are rewritten by every tier run and two more runs followed; review caught it, in the row that exists to warn about exactly this. The readings: fast tier **68.92 / 93.86 / 106.3 / 63.18**, then **72.75** and later **59.51** on the settled tree; full tier **207.54** while hot, then **191.61** and **179.59** after. The probe readings below are transcribed from `Get-Process` / `Get-CimInstance` output, also not retained. On `fix/780-783-class-constant-owner` the fast tier read **68.92 s** on the commit that carries the whole fix, then **93.86 s** on a seven-line JSON addition and **106.3 s** on no change at all. The foreign-job procedure above was run FIRST and came back clean -- every python and node process idle at under 3 CPU-seconds total, 13.1 GB free of 31.7, `LoadPercentage` 1, `CurrentClockSpeed` at 100% of `MaxClockSpeed`. jjg cleared the obstruction and the same tree read **63.18 s**, the fastest of the day. ⚠⚠ **Every software probe this box exposes read HEALTHY while the machine was throttling**, including the clock, which reports the 2100 MHz BASE against itself and therefore always reads 100% -- it cannot see turbo being withheld, which is exactly what thermal throttling takes away first. `MSAcpi_ThermalZoneTemperature` is `Not supported` here, so there is no temperature to read either. ⚠ The tell is the SEQUENCE, not any single reading: three runs rising monotonically on a tree that stopped changing. A rising series with a clean foreign-job probe is the physical case, and the remedy is to look at the hardware before spending fifteen minutes idling. ⚠ **FIFTH SERIES 2026-09-22, recorded for the count, provenance the same as above: every fast-tier number below is transcribed from the commit hook's refusal text or a one-off stdout and none can be read back** (`evidence/fast.md` is rewritten by every run; the one retained reading is the last). On `fix/797-c-struct-fields` the hook refused five commits on `suite.fast_seconds` alone -- **107.71, 110.17, 109.2, 95.62, 96.96 s** against 90, a standalone run between them at **114.41** -- with every test passing each time (1404 passed, 7 skipped), then passed at **71.86** and **67.68**; `feat/f35-name-the-failures` passed first time at **64.81** and then **68.74** -- and the first of those is ALREADY unreproducible, because the second run rewrote `evidence/fast.md` before this row was reviewed: the first draft of this sentence called 64.81 retained, which is the mistake this row records twice above, made a third time in the row that warns about it. Both are transcribed. The box measured idle during the later misses (CPU 3-14%, 13 GB free, clock at maximum, the other session's model benchmark at 0 CPU-seconds), so the foreign-job probe was CLEAN and the readings still fell 20% over. A `Microsoft-Windows-Kernel-Power` event 105 (a power-source change) at 15:34:02 lines up with the first miss; the tier read 71.86 s twenty minutes before it. Per-test durations showed no single test ballooning -- the whole set was uniformly ~50% slower, which is a clock and not a lock. Twelve refusals over two branches in two days now, every one with a green test set. The Floor does not move; the two candidates one sentence up are unchanged.|
| F-34 | **`harness fast` exits non-zero on a CORPUS CHECKSUM mismatch, which prints no row in the summary table -- so the artifact reads all-PASS under a FAIL header, the exact signature W-42 documents for a killed hook.** On 2026-09-21 a commit of the #778 Go fix was refused with `fast tier FAIL:` followed by the single line `HARNESS FAIL`, over an `evidence/fast.md` whose sixteen Floor rows were every one PASS, `suite.fast_seconds` at 71.64 against 90, and a trailing `1402 passed, 7 skipped ... All checks passed!`. **The cause was three lines above the first Floor row in stdout and absent from the summary entirely:** `MISMATCH goldset_gold (benchmarks/goldset/gold.json)`, because the fix deliberately moved a Go method's id (`queue.go::Push#method` -> `queue.go::Queue.Push#method`) and `harness/corpora.json` pins that file's sha256. ⚠⚠ **The failure text cannot name it either**: `pre_commit.py` builds its message by filtering harness stdout for lines containing ` FAIL` or `failed`, and a mismatch line contains neither, so the tail collapsed to the bare verdict. ⚠ **The near-miss is the diagnosis, not the defect.** A repin is one line and correct here; what cost the time was reading the all-PASS table as W-42's pending-summary case and concluding the box was slow -- a wrong answer that has been RIGHT seven times on this machine (F-19, F-33), which is what made it cheap to reach for. Two runs were spent on it before the mismatch was read. | `harness/corpora.py::verify`, `.claude/hooks/pre_commit.py` (the ` FAIL`/`failed` filter), `harness/corpora.json` | OPEN, and the pin itself is not the problem. Two candidates, both in the messenger: the summary table gains a corpora row so the artifact can express the failure it is refusing over, and the hook's tail filter keeps any line containing `MISMATCH` (or, better, stops filtering when the filter yields nothing and prints the last lines it has). ⚠ Until then, the rule for an all-PASS table under a FAIL header is to read harness stdout from the TOP, not to reach for the clock: W-42's pending-summary case and this one are indistinguishable in the artifact. |
| F-35 | **A contended full tier reports a FAILURE COUNT and `full.md` cannot say which tests failed, so the outlier is unreproducible by construction.** On 2026-09-22, on `fix/786-rust-struct-fields` at commit `203fb608`, `run_full.py` reported `3 failed, 11889 passed, 24 skipped in 1748.54s (0:29:08)` and `suite.full_seconds` FAIL at 1749.79 against a 360 floor. The same tree, same command, minutes later on a quiet box: `11892 passed, 24 skipped in 177.17s`, then `186.26s` PASS under the hook. **A 10x wall-clock difference, and three failures that never reproduced and cannot be named** -- the arithmetic reconciles either way (11,889 + 3 + 24 = 11,916 = 11,892 + 24), so the three are accounted for as passes on the quiet run and as failures on the contended one, and nothing retained says which tests they were. ⚠ A first draft of this row called them failures that DO NOT EXIST, which is the overclaim this row exists to warn against: the evidence supports unreproducible and unnameable, not nonexistent. Corrected in review. The F-33 family (a busy box, not a slow tree), one tier up. 29 python/node processes were resident at the time -- ⚠ that reading is transcribed from a `ps` count taken while the contended run was in flight and, like F-33's probe readings one row up, is not retained and cannot be read back; it identifies the shape, not the culprit. ⚠⚠ **The actionable half is the ARTIFACT, not the box.** `evidence/full.md` records the pytest summary LINE and nothing else: no `FAILED` ids, no short test summary. So a run that fails three tests leaves no record of which three, and the only way to find out is to reproduce -- which is exactly what a contended run will not do. **A count is not a diagnosis**, and this is the same shape F-33 records about its own numbers being transcribed rather than retained. ⚠ The re-run is the remedy and the floor does not move; both readings are recorded here because a single 29-minute run with an unnameable failure set is otherwise indistinguishable from a real regression. | `.claude/hooks/run_full.py` (what it writes into `evidence/full.md`), `harness/thresholds.json` `suite.full_seconds` | FIXED 2026-09-23 (the Floor did not move): both pytest tiers print every `FAILED`/`ERROR` id on a non-zero run (`_failed_id_lines`, capped at 25 with the remainder DISCLOSED by count) and `_Tee.summary_markdown` collects exactly those lines into a `**Failed:**` block, absent on a clean run; `_annotate_failure` reads the same rows, so the console, the artifact and the Checks tab are one derivation. `tests/test_a_failing_tier_names_its_failures.py`. ⚠ **A claim in the first plan was wrong and is corrected here rather than deleted:** the console tail was never the loss. The coverage table prints BEFORE the short summary, so the names sat in the last ~150 characters of stdout on every failing run, and they were discarded by piping the runner through `tail -5` -- twice. The artifact was the honest scope all along. ⚠ `.claude/state/` is gitignored, so an evidence path cited from here resolves for nobody else; the readings above are inline for that reason (F-33's own correction). |
| F-36 | **The ledger's side of W-49, which is the record.** `test_edit_guard` demanded a `harness/retired.json` row for a CONDITIONAL `pytest.skip(` added beside a test this branch had itself introduced, so nothing was retired and the row would have been a FALSE entry -- and this is the one file where a false entry is worse than a warning, because every later reader treats it as history. The hook's rule, its two blind spots and why the obvious remedy fires identically are in `docs/workflows/FINDINGS.md` **W-49**, beside W-30 on the same hook, which is where a reader of `test_edit_guard.py` lands. ⚠ Kept to a pointer deliberately: the first draft restated three of W-49's claims here, nothing in this tree watches two FINDINGS rows for drift (`test_key_files_split.py` and `test_cli_env_split.py` exist because that has been paid for twice), and one copy of each claim is not optional. |
| F-37 | **DoD 11's "in its own commit" is unenforced, and the record shows it.** STANDARD.md item 11 asks for a `harness/retired.json` entry in its own commit; `dod_checklist.py` row 11 checks that the entry EXISTS and the ledger test passes, never which commit carries it, and `pre_commit.py` runs on one commit at a time and cannot see the split. On 2026-09-23 the #833 fix (`b1ffdbba`) carried its retirement entry beside `src/` and the tests, exactly as `fbd6b5b0` (#812's audit retirement) had the day before; both were caught by a reviewer reading `git show --stat`, not by any gate. ⚠ The rule's purpose is that a retirement is REVIEWABLE apart from the fix; on a squash-merged branch the two commits collapse to one on `main` regardless, so the only reader the split serves is the PR reviewer, who has the ledger diff in hand either way. ⚠⚠ Recorded rather than silently tolerated: a rule that two consecutive fixes broke without a gate firing is either a rule the checklist should enforce (row 11 reads `git log -- harness/retired.json` and refuses an entry whose commit also touches `src/`) or a rule to strike from STANDARD.md; a third unenforced instance should settle which. ⚠⚠ **Measured on the third review round of #833: the clause is UNSATISFIABLE under the commit hook, not merely unenforced.** `tests/test_retirement_ledger.py::test_every_ledger_entry_names_a_test_that_actually_left_this_branch` runs in the fast tier that `pre_commit.py` requires of every commit and compares the working tree against the merge base, so a ledger entry committed BEFORE the deletion names a test that has not left (refused), and the deletion committed BEFORE the entry is a retirement with no ledger row (refused). The two halves can only arrive together, which is what W-31/#797 already recorded about the hook's timing. The rule and the gate contradict each other, and the remedy is a STANDARD.md edit (strike "in its own commit", or say "in the same commit as the deletion and no other change"), which is the maintainer's. | `docs/standard/STANDARD.md` item 11, `.claude/hooks/dod_checklist.py` row 11, `tests/test_retirement_ledger.py` | OPEN, handed to the maintainer with #833's PR; the two instances are `fbd6b5b0` and `b1ffdbba`. |

## Product-code seams

One, after the build (2026-09-03, F-13): `src/jcodemunch_mcp/tools/search_symbols.py` ranks tied scores by symbol id instead of encounter order. Everything in the build itself was `benchmarks/`, `tests/`, `harness/`, `.github/workflows/`, `.gitignore`.

## Tests that fail against current code

- none since F-01 was fixed (2026-09-03). F-14, F-15 and F-16 were harness defects seen only on a fresh runner, all fixed the same day.
