# FINDINGS — what the workflow layer needed and did not have (2026-09-05)

Each entry names what a workflow needs from the harness, a script or Claude
Code, how the workflow degrades until it exists, and its status. Rule from
the brief: workflows do not modify the harness, CI or product source; they
log the gap here and degrade gracefully.

| ID | Finding | Where | Degradation | Status |
|---|---|---|---|---|
| W-1 | `scripts/surface_diff.py` diffs tool NAMES only. A description, argument or tier change is a surface change (STANDARD criterion 4; DoD 4) and is invisible to it, so `surface_guard` cannot see the change class that grew `core_compact` to within two tokens of its ceiling. | `scripts/surface_diff.py`, H3 | `/feature` step 7 dumps `_build_tools_list()` name→description JSON from both trees and diffs it into `evidence/surface_descriptions.md`; `dod_checklist.py` marks DoD 4 with "description diff not captured (W-1)" when the dump is absent; the reviewer counts an absent dump as unmet. | OPEN; a `--descriptions` flag on the script would close it |
| W-2 | `scripts/pr_bench_comment.py` renders base-vs-PR rows for the latency and token ids only; fidelity, replay, goldset, route and schema Floors have no base value outside `harness/results/latest.json`. | `scripts/pr_bench_comment.py` | `/benchmark-compare` step 4 renders the remaining rows from the two `latest.json` files; a value absent on either side prints `n/a`, never 0. | OPEN |
| W-3 | The harness has no stamp-file output for "this tier passed on exactly this tree". `--write-results` writes the TRACKED `harness/results/latest.json`, which a branch must not dirty, and records `env.commit`, not the working-tree identity. | `harness/__main__.py` | `.claude/hooks/run_full.py` wraps the full tier, writes `.claude/state/full-tier.json` with the tree id (HEAD tree + digest of the uncommitted diff) before and after the run, and invalidates the stamp if the tree moved during it. | OPEN; a `--stamp-file PATH` would remove the wrapper |
| W-4 | No machine-readable list of LOAD-BEARING test files; H2 parses `docs/harness/ARCHAEOLOGY.md`'s table rows by filename. | `docs/harness/ARCHAEOLOGY.md` | Acceptable: `tests/test_retirement_ledger.py` parses the same table, so the format is already load-bearing. | ACCEPTED |
| W-5 | Claude Code 2.1.260 has no commit or PR hook EVENT and a `PreToolUse` hook cannot see the commit message. "The commit message states the lesson" (DoD 11) is therefore the reviewer's check over `git log`, not H2's. | Claude Code | Documented substitution (DESIGN D4); H1/H4/H5 match on the Bash/PowerShell command text. A `git commit` typed by the human outside the agent is not hooked, and never was. | ACCEPTED |
| W-6 | Reviewer determinism is not controllable from a prompt. The fixed output format makes drift visible line by line; it cannot make it impossible. | `.claude/agents/reviewer.md` | Phase 4 runs the same diff twice in fresh contexts and diffs the verdicts; the record is in `VERIFICATION.md`. | MEASURED in Phase 4 |
| W-7 | The hook JSON `additionalContext` channel used for WARNINGS (exit 0) is shown to the model as context, not to the human in the transcript; a warning the model ignores is invisible to jjg. Blocks (exit 2) are shown to both. | `.claude/hooks/_common.py` | Every WARNING also names itself in the first line so it is greppable in the session log; a budget overrun is additionally written to `.claude/state/evidence/hook_warnings.log` (added 2026-09-05). | OPEN; a Claude Code option to surface `additionalContext` to the user would close it |
| W-8 | The project `settings.json` hooks apply to EVERY session in this directory, including the one building them: the first `gh pr create` smoke test blocked the very Bash call that carried the test payload, because H4 matched the command text. | `.claude/settings.json` | Tests drive the hooks through a Python runner with the payload built in code (`tests/test_workflow_hooks.py`), never through a shell line that contains the trigger verb. Documented in `VERIFICATION.md` §1. | ACCEPTED (it is the hook working) |
| W-9 | `dod_checklist.py` DoD 7 (background-behaviour disclosure) and DoD 8 (`*_basis` beside a rate) are regex heuristics over the diff (`threading.Thread`, `socket.socket`, `httpx.`, keys ending `_pct/_rate/_share`, `confidence`). A new spelling of either is invisible to them; the reviewer's prompt carries the property. | `.claude/hooks/dod_checklist.py` | The checklist row says which regex fired or did not; the reviewer re-verifies each row (prompt step 1). | ACCEPTED; `tests/test_standard_invariants.py` is the durable half for DoD 8 |
| W-10 | The `release` skill is now TRACKED here and COPIED at `C:\MCPs\.claude\skills\release\` for jdoc/jdata (DESIGN D2). Two copies drift; the suite copy has no test. | suite `.claude/` | The tracked copy carries a REVISED banner naming the suite copy; a suite-level rule ("edit the jcm copy, sync outward") is the next layer's. | OPEN |
| W-11 | H1 reads the INDEX to decide whether a commit touches code, and `git add -A && git commit` in one Bash line runs the hook BEFORE the add, so the index is empty and the first commit of this layer (095a80e, tests/ and .claude/ staged in the same line) passed in 0.2 s without the fast tier. | `.claude/hooks/pre_commit.py` | When the command text also contains `git add` or `commit -a`, everything modified or untracked counts as staged. Recorded because it is the vacuous-pass shape the layer exists to refuse; found by the commit taking 0.2 s. | FIXED 2026-09-05 |

