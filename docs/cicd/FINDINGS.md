# CI/CD FINDINGS (branch series `cicd/*`, 2026-09-03)

Rule from the brief: if CI cannot run something the harness requires, or the
design met a fact the audit did not know, it is recorded here, never worked
around by weakening the harness.

| ID | Finding | Where | Status |
|---|---|---|---|
| C-1 | `ruff format --check src/` would reformat **254 of 284 files**. A formatting gate over `src/` is therefore a one-commit mass rewrite first, which is the same call as `ruff check tests/` (292 findings, ENFORCEMENT-PLAN item 6) and not this series' to make. The `fast: format` gate covers `harness/` and `scripts/` (formatted in this change); `src/` is named in the job title. | `pr-gate.yml` fast: format | OPEN: jjg's call on a `ruff format src/` commit; the gate widens to `src/` in the same PR |
| C-2 | DESIGN §5 said the dependency Floor is "zero HIGH or CRITICAL". `pip-audit` (OSV via PyPI) reports **no severity**, so a Floor by severity is not measurable with it. The Floor is `deps.vuln_max = 0` over EVERY advisory in the runtime set, with a dated allowlist (`harness/audit-allowlist.json`, empty) for a finding that cannot be fixed by a bump. Stricter than designed, not looser. | `harness/__main__.py::_measure_deps` | CLOSED by the stricter Floor |
| C-3 | The first dependency audit found `click 8.3.1` **PYSEC-2026-2132** (fix 8.3.3+). Bumped to 8.5.0 with `uv lock --upgrade-package click` under uv 0.12.1 (CI pins 0.9.5; CLAUDE.md warns a newer local uv once rewrote 53 unrelated markers). The diff was checked by hand: click's version, sdist and wheel lines, and the drop of its `colorama` conditional dependency, which is upstream's change in 8.5. Nothing else moved. | `uv.lock` | CLOSED |
| C-4 | pyright over `src/` reports **369 errors** at 1.1.405. Recorded as the ratchet Floor `types.error_max`; it may only fall, by `--stamp` after a fix. The count is a fact about the tree, not a target. | `harness/thresholds.json` | OPEN: the number; CLOSED: the gate |
| C-5 | The packaging smoke test cannot rely on JSON from the installed server: `server_output` defaults to `adaptive`, and `JCODEMUNCH_DEFAULT_FORMAT=raw` did not switch `search_symbols` off the MUNCH table encoding in the probe (`index_folder` did return JSON). The probe parses either shape for the one id it needs. Whether the env override is honoured per tool is a product question, not a CI one. | `scripts/handshake.py::_first_hit_id` | OPEN as a product question; the probe is format-independent |
| C-6 | `shutil.which("jcodemunch-mcp")` on this box resolved to the GLOBAL 1.108.314 install in `Program Files` even with the venv's `Scripts` first on `PATH` under Git Bash. The probe takes `--command <exe>` and the workflow passes the venv's binary explicitly; a `which` lookup was the #536 shape again (probing something other than the artifact). | `pr-gate.yml` stage 3 | CLOSED |
| C-7 | `.github/actions/health-radar/action.yml`, the composite action EXTERNAL consumers pin as `health-radar-v1`, used `actions/setup-python@v5` by tag. Pinned to the v5 SHA in this change. Its header says any edit is a tagging obligation (`health-radar-v1.0.2` + move `health-radar-v1`); tags are out of this session's scope, so consumers keep the unpinned copy until jjg tags. | `.github/actions/health-radar/action.yml` | OPEN: tag it |
| C-8 | `gh workflow run release.yml --ref cicd/release` answers `HTTP 422: Workflow does not have 'workflow_dispatch' trigger` while the file on that ref has one: GitHub resolves a dispatchable workflow from the DEFAULT branch. The release dry run can be dispatched only after `release.yml` is on `main`; Phase 4's dry-run proof is therefore the first act after the step-5 PR merges. | `release.yml` | OPEN until the merge; then the dry run |
| C-9 | The full-tier wall Floor `suite.full_seconds` (360 s, 2x this box's median) fails EVERY GitHub-hosted windows leg: 478.7 / 483.55 / 549.12 / 553.55 s of pytest on one commit (PR #578). Not a regression -- the runner is ~3x slower -- and not a reason to loosen: a NEW platform-scoped Floor `suite.full_seconds_ci_windows` = 1033 (2x the median of those four) applies when `GITHUB_ACTIONS` is set on win32; the 360 stays for every other run. `timeout-minutes: 20` on the job holds (observed job wall 8.5-9.5 min). | `harness/thresholds.json`, `harness/__main__.py::_full_wall_floor_id` | CLOSED by the new Floor; tightens after three more CI runs |
| C-10 | CodeQL (`security.yml`, actions queries) reviewed the release PR itself and found two things in `release.yml`: an unquoted colon in a step name (`name: Dry run: no upload...`), which made the file INVALID YAML and would have made the release workflow never run and never fail; and `actions/checkout` of `needs.preflight.outputs.sha` flagged as an untrusted checkout. The first is fixed and `tests/test_workflows_pinned.py` now parses every workflow. The second is dismissed with a comment: the job runs only from `workflow_dispatch` (repo refs, write access required) and the `push: tags` path fails in the pre-flight before any checkout. Required conversation resolution held the merge until both were answered. | `release.yml`, code-scanning alerts 1 and 2 | CLOSED; the gate caught its own author a third time |
| C-11 | CodeQL's first `security-and-quality` run over `src/` and `tests/` reports a baseline of roughly 650 open alerts (top rules: `py/unused-import` 245, `py/empty-except` 151, `py/unused-local-variable` 87, `py/unused-global-variable` 41). None is an error-severity security finding; they are the quality queries. DESIGN §5 planned to make `codeql` a required check after two weeks; with this baseline the switch needs either a triage that dismisses the quality noise or a `queries: security-extended` narrowing first. | code-scanning alerts | FIXED 2026-09-06: `queries: security-extended` (jjg's call); the quality notes leave the security page and the alerts carrying a security severity (read them from the code-scanning alerts query, never from here) remain the list to triage; still INFORMS |
| C-12 | `license/cla` is a legacy commit STATUS posted by a webhook. A merge commit pushed to a PR branch by `update-branch` or a local `git merge origin/main` arrived with the status MISSING on the new head (delivery answered 200 and posted nothing), and with `license/cla` required the merge read BLOCKED with every check-run green. Remedy is the documented one (CLAUDE.md policy 3d): redeliver that head's `pull_request/synchronize` webhook delivery; it posted within 10 s. Never post the status by hand. | PR #584 head 548fe91 | CLOSED; the runbook §8 names the remedy |
| C-13 | The release pre-flight's `ci` check read branch protection to learn the required contexts; inside Actions the `GITHUB_TOKEN` gets HTTP 403 on that endpoint (no `administration` scope exists for workflow tokens). Under the dry run it was reported as FAIL-not-blocking; under a real release it would have BLOCKED every release (UNKNOWN blocks). Fallback added: when protection is unreadable, every check-run on HEAD must have concluded success/neutral/skipped and the gate's job families must be present by name. Names are not Floors. | `scripts/release_preflight.py::all_runs_verdict`, dry run 33826479351 | CLOSED; proven by the second dry run |
| C-14 | The C-13 fallback still could not pass: on a MAIN commit the check-runs are `main.yml`, `security.yml` and the release workflow's own jobs; the PR gate's jobs ran on the PR's merge ref, a different SHA, so `fast:`/`full:`/`package:`/`done:` are never present on HEAD and the second dry run reported `gate jobs absent on HEAD`. The pre-flight now asks the right question for a main commit: branch protection with `enforce_admins` is what guarantees the gate passed, and the witnesses that DO run on main -- `main: harness full (ubuntu, 3.12)` and `main: harness bench (online)` -- must have concluded success, with no failed run of any other name and the release's own jobs ignored; a witness still running is waited for (up to 20 min), never treated as passed. The old required-context rule is deleted rather than kept beside it. | `scripts/release_preflight.py::main_witness_verdict`, dry run 33827504322 | CLOSED; proven by the third dry run |
| C-15 | The first real release (run 33832239480, 2026-09-04) reached `release: pypi` and was refused: `invalid-publisher: valid token, but no corresponding publisher`. The token's claims were exactly the design's (`repo:jgravelle/jcodemunch-mcp:environment:pypi`, `release.yml@refs/heads/main`) and Test PyPI accepted the same token minutes earlier, so the mismatch was on PyPI's side: the project's publishing page accepted the form and then listed `No publishers are currently configured`, repeatedly. Cause unknown (a PyPI-side persistence or 2FA-confirmation step that the page does not show). The release was finished by hand with the existing `~/.pypirc` token: the CI-built artifact from the same run was downloaded, `twine check`ed and uploaded by jjg, then the post-publish handshake (fresh venv from PyPI, fixture, tool count 91) was run locally and the GitHub release created from the CHANGELOG block with the same two files. | PyPI publishing page; run 33832239480 | OPEN, publisher listed 2026-09-11: jjg re-entered the form (owner `jgravelle`, repository `jcodemunch-mcp`, workflow `release.yml`, environment `pypi`) and the publishing page lists it on reload, which it never did on 2026-09-04 or 2026-09-11 morning. Unproven until a dispatched run passes `release: pypi`; the next release is that proof, and RUNBOOK §1a stays until then. The `~/.pypirc` token is revoked only after one release goes through OIDC end to end (§5 step 4) |
| C-16 | The code-scanning page after C-11's narrowing: every alert carrying a security severity was read at its line on 2026-09-07 (the `code-scanning/alerts` query, `security_severity_level` set). Seven were defects and are fixed on this branch: three `actions/code-injection` in `speedreview/action.yml` (inputs interpolated into `run:` text; env now), two `py/bad-tag-filter` in the Razor and Astro block regexes (a `</script >` did not end the block), `py/overly-permissive-file` on the process-lock create (0o644; 0o600 now), `py/jinja2/autoescape-false` in the munch-bench leaderboard. Eleven are dismissed with the reason in the alert: four `py/clear-text-logging-sensitive-data` where the taint is the discovery skip-reason key named `secret` flowing through an index result dict (a count, never a value); three `actions/untrusted-checkout` on inbound workflows that fetch a PR ref as data and never check it out at the root, or execute the merge ref by design under POLICY's bench exemption in a read-only job (`inbound-bench-full`); two `py/overly-permissive-file` and one `py/clear-text-storage-sensitive-data` on test fixtures under a temp dir; one `py/incomplete-url-substring-sanitization` on a test assertion over captured stdout. | `tests/test_code_scanning_triage.py`; the alert numbers in that file's docstring; the dismiss comments on the alerts | FIXED 2026-09-07 for the seven; the eleven dismissals are a human's API calls (the layer posts nothing) and are listed in the PR body. OPEN: the leaderboard fix's behavioural test needs `jinja2`, which is in the `bench` extra the PR gate does not install, so on CI only the source-level assertion of `autoescape=True` runs; the behavioural arm runs under `uv run --with jinja2` (green.txt) |
| C-17 | `main.yml`'s Monday job "results PR (weekly, Mondays)" failed on its FIRST firing (run 34156947334 on 2937e8df, 2026-09-07): `git push -u origin harness-bot/results-2026-09-07` was rejected with `GH013 ... Cannot create ref due to creations being restricted`. The inbound ruleset (id 22311729, RUNBOOK section 9) restricts `creation`/`update`/`deletion` on every branch except `inbound/**`, `inbound-ledger` and `main`, with bypass for repository roles only, and the job pushes as `github-actions`, which holds no role. Two layers designed a week apart each assumed the other's branches did not exist; the full and bench jobs of the same run passed, so no Floor is involved and no `regression` issue opened. | `.github/workflows/main.yml` lines 168-183; `gh api repos/jgravelle/jcodemunch-mcp/rulesets/22311729` (the exclusion list) and `gh api repos/jgravelle/jcodemunch-mcp/actions/permissions/workflow` (the setting) reproduce the evidence live | Ruleset half FIXED 2026-09-07: `refs/heads/harness-bot/**` added to the ruleset's `ref_name.exclude` (the human's edit); the dispatched witness run 34158092318 pushed the branch. Second cause behind the first, same run: `gh pr create` failed with `GitHub Actions is not permitted to create or approve pull requests` because the repository's Actions setting (`actions/permissions/workflow`, `can_approve_pull_request_reviews: false`) forbids `GITHUB_TOKEN` from opening a PR; the branch exists and the PR does not. Setting half FIXED 2026-09-07 (the human's edit: `can_approve_pull_request_reviews: true`, `default_workflow_permissions` still `read`); this Monday's PR (#633) was opened by hand from the pushed branch with the job's title and body. Third cause, found on #633's gate (BLOCKED with every check green and no `license/cla` status, `commits/c25107a7/status` count 0, no CLA comment): the job committed as `harness-bot <harness-bot@users.noreply.github.com>`, and a noreply address resolves to whichever account owns that login. `users/harness-bot` is a real account created 2026-05-18 that is not ours, the API shows the commit's author as that login, and CLA Assistant saw an unsigned stranger. `release.yml`'s tagger `release-bot` is a real account since 2014, same shape. Both now commit and tag as `github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>` (fixed in this PR). Outcome for this week: a redelivery of #633's open event made CLA Assistant post `license/cla=pending: not signed` on c25107a7 (the stranger is unsigned, as expected), #633 was closed with the reason, and the same tree was re-authored as `github-actions[bot]` with git plumbing (`commit-tree` on c25107a7's tree over main, no edit, no checkout) and pushed as `harness-bot/results-2026-09-07-b` (998c0cd8) for a replacement PR. `tests/test_workflow_commit_identity.py` is the ratchet: every `user.email` in `.github/workflows/` must be the numeric `<id>+<login>@users.noreply.github.com` form, with the five `inbound@` sites allowlisted by site under inbound FINDINGS IN-20 (their fix is the App's own address; fixed 2026-09-08, the allowlist is empty). Witnessed the same evening: after #634 merged, the push run (34166359635) was rejected on the stale branch as predicted; the human deleted it and dispatched (34166810520, 22:28Z), which pushed the branch and opened #635 at 22:30Z as `github-actions[bot]`: the witness. #635 was then closed and its branch deleted (22:36:08Z, a command meant for the stale branch, run from a second session), and a second dispatch (34167239425, 22:36:19Z) re-created the branch and opened #636 at 22:37Z; both runs' results job succeeded, so causes one to three are closed. The re-authored branch `harness-bot/results-2026-09-07-b` (998c0cd8) is superseded by #636 and has no PR; deleting it is a remote branch deletion, the human's. Fourth cause, behind the third: CLA Assistant posted `license/cla=pending: not signed` on the bot's own commit (d2891564 and 4871c9d4), because `github-actions[bot]` is not on the CLA allowlist (RUNBOOK 9 put only the inbound App's login there). OPEN for that one: the allowlist entry is the human's, at cla-assistant.io; then a redelivery of #636's open event posts the status |
