# FINDINGS — what the inbound layer needed and did not have (2026-09-04)

What the harness, the CI gates, the workflow layer, the product, or the
repository's settings would have to expose for the jobs in `DESIGN.md` to
do their work, and what this layer could not change itself. Rows are
appended, never rewritten; a fix changes the status column only.

| ID | Finding | Where | Remedy | Status |
|---|---|---|---|---|
| IN-1 | `.github/ISSUE_TEMPLATE/multi_finding_report.md` declares `labels: qa`, and no `qa` label exists, so every multi-finding report has arrived unlabelled since the template shipped. | repository labels | Create the label (Phase 4 item 1 does). | FIXED 2026-09-04 (label created) |
| IN-2 | An environment named `copilot` exists with no reviewers, no reference anywhere in the tree, and no record of who created it. | repository environments | A human names its purpose or deletes it; this layer adds no environment of its own and must not inherit one it does not understand. | OPEN (human) |
| IN-3 | `license/cla` is a required status on `main`, posted by CLA Assistant for the PR author's account. Whether it posts for a GitHub App's PRs is unverified. An agent-authored PR that never receives it cannot merge even by a human's click. | branch protection; cla-assistant.io allowlist | Add the `jcodemunch-inbound` App login to CLA Assistant's allowlist, then open one PR from the App and read the status (VERIFICATION 1.6). | OPEN (human; blocks DESIGN item 6) |
| IN-4 | Private vulnerability reporting could not be verified enabled from the API; zero advisories have ever been filed while six security-shaped findings arrived in public issues. `SECURITY.md` names the advisory form as the route. | repository settings; SECURITY.md | Enable private vulnerability reporting if it is off; submit one test advisory and record its number (VERIFICATION 1.7). Until then POLICY 6.3's security notification is the label only. | OPEN (human) |
| IN-5 | `health-radar-comment.yml` posts an artifact produced by an untrusted `pull_request` run using a write token. The payload is our own script's rendering of our own numbers, so the exposure is a crafted PR that alters what our script renders. Pre-existing; not this layer's. | `.github/workflows/health-radar-comment.yml` | Render the comment in the trusted job from the raw numbers rather than from the untrusted run's Markdown. Logged for the CI layer (`docs/cicd/FINDINGS.md`). | REPORTED |
| IN-6 | `sha_pinning_required` is off at the repository level; `tests/test_workflows_pinned.py` enforces the pin, GitHub does not. | repository Actions settings | Turn the setting on (Settings > Actions > General); a second guard costs nothing. | OPEN (human) |
| IN-7 | The official action refuses `issues` events from actors without write access, which is every reporter on record, and a chained `workflow_run` inherits the actor. | `anthropics/claude-code-action` trigger checks | DESIGN D1: record on the event with no model, process on a schedule. Not a defect in anything of ours; recorded as the substitution it forced. | ADAPTED |
| IN-8 | The shared Claude GitHub App's token carries Actions and Workflows write, which POLICY 4.4 forbids a headless job to hold; `GITHUB_TOKEN` commits do not trigger the PR gate. | GitHub App permissions | DESIGN D2: a custom App with Contents, Issues, Pull requests. Creating it, its two secrets and `ANTHROPIC_API_KEY` is a human step. | OPEN (human; blocks DESIGN items 2 to 7) |
| IN-9 | `claude -p --bare`, the documented CI mode, skips `.claude/commands/`, `.claude/agents/`, hooks and CLAUDE.md, i.e. the whole workflow layer including the deny guard. | Claude Code CLI | Every headless run is non-`--bare` in a checkout whose `.claude/` came from `main`; the action restores `.claude/` from the base branch on PR events, which VERIFICATION must exercise. | ADAPTED |
| IN-10 | `pyproject.toml`'s sdist exclude list does not name `.github/`, so the inbound helper scripts and prompt files ship in the sdist unless hatchling's gitignore handling drops them (it does not; `.github/` is tracked). | `pyproject.toml [tool.hatch.build.targets.sdist]` | Add `.github/` to the exclude list in the plumbing PR (packaging config, not product source) and assert it in `tests/test_inbound_plumbing.py`. | see VERIFICATION 1.8 |
| IN-11 | `dod_checklist.py` rows 1 and 3 key on `src/` (workflows W-35), so a headless fix whose change is test-only would read `n.a.` on both and the self-check cannot read "met" from the checklist alone. | `.claude/hooks/dod_checklist.py` | The self-check reads `red.txt`/`green.txt` and the CHANGELOG diff directly (DESIGN 5b, 5c) rather than the checklist rows; W-35 remains the fix. | ADAPTED |
| IN-12 | `tests/test_sdist_exclusions.py::test_no_unexpected_file_at_the_sdist_root` fails on a developer box while a full-tier run is writing `.coverage.<host>.<pid>.*` into the repository root; the allowlist is right, and a release cut during a coverage run would ship those files. Found by the plumbing PR's reviewer. Pre-existing; belongs to the CI layer. | `harness` coverage output location; `docs/cicd/FINDINGS.md` | Point pytest-cov's data file at the scratchpad or `.claude/state/`, or have `run_full.py` clean the root after the run. | REPORTED |
| IN-13 | `anthropics/claude-code-action` has no `prompt_file` input (the first triage draft assumed one) and its only documented output is `structured_output`. The runner renders the prompt into a step output and passes `prompt:`; the model's JSON is written to a path-restricted file and validated by `apply_triage.py`, so a malformed or missing result escalates rather than being parsed out of a transcript. | `.github/workflows/inbound-triage.yml` | Recorded so the next job does not repeat the assumption. | ADAPTED |
| IN-14 | The PR gate writes every tier's summary to `$GITHUB_STEP_SUMMARY` and uploads only `dist-<sha>` and `harness-bench-<sha>` as artifacts, so a job that is not the gate has no summary file to read and the first depeval draft's `floors_hold` returned UNKNOWN on every real run (`agent:ready-to-merge` unreachable; found by the item-4 reviewer). The verdict lines exist in the gate's JOB LOG (`gh run view --log`, one `<id> crit <c> floor <cmp v> observed <o> PASS|FAIL` line per Floor; 33 such lines counted with `gh run view <id> --log | grep -cE 'crit [A-Z]+[0-9]* *floor'` on the latest PR-gate run at the time of writing, 2026-09-04). | `.github/workflows/pr-gate.yml` (layer 4, not this layer's to change) | `apply_depeval.floors_hold` parses the job log and any Markdown table; the log is fetched with the read-only `GITHUB_TOKEN` (`actions: read`). A gate that uploaded its summaries as an artifact would be simpler and is the CI layer's call (`docs/cicd/FINDINGS.md`). | ADAPTED |
| IN-15 | DESIGN 6 step 5 says any `inbound:queued` older than 2 hours is named in the digest; the sweep's summary carries `stale_needs_human` (step 4) and nothing for step 5, and the digest renders what the summary carries. Found 2026-09-04 building item 7. | `.github/inbound/sweep.py` | Add the query to `sweep.py` (`gh issue list --label inbound:queued`, the `stale_needs_human` shape) and a row to the digest; until then the digest does not claim the list. | OPEN |
| IN-16 | CodeQL flags `actions/untrusted-checkout` on `inbound-depeval.yml` (critical: the classify job fetches `workflow_run.head_sha` and reads `uv.lock`/`pyproject.toml` from it with `git show`) and on `inbound-bench-full.yml` (medium: the bench job runs the PR's tree in a worktree). Both are the design: the depeval read is data only on a read-only token, never checked out at the workspace root (`test_pr_code_is_never_checked_out_at_the_workspace_root`); the bench executes PR code by design, on a read-only token with no App token in the job (`test_a_job_that_executes_pr_code_holds_no_app_token`), started only by a maintainer's `agent:bench-pending` label on a Dependabot head (DESIGN 4). CodeQL is not a required check. Found 2026-09-04 reading the stack's checks before merge. | `.github/workflows/inbound-depeval.yml`, `inbound-bench-full.yml` | Dismiss the two alerts as "won't fix" with this row as the reason, or leave them open as a standing reminder; either is a human's call. Three Python alerts on the same run (two side-effecting asserts in `tests/test_inbound_prompts.py`, an uncommented `except` in `ledger.py`) are fixed on `inbound/docs`. | ADAPTED |
| IN-17 | `budget.py` counted every run of the job created today, so a run that declined at its gate consumed the same daily slot as one that worked; the second live sweep (run 33939144859) was declined for the first (33936406280), `runs_per_day: 1 of 1 used`, with the switch fixed and the day otherwise empty. The asking run itself was also counted (fixed earlier the same day, VERIFICATION 7.2). | `.github/inbound/budget.py` | `run_did_work` reads each run's steps (`gh run view --json jobs`) and counts a run only when a step outside the plumbing set (setup, actions, post steps, the token, the gate, the audit record) succeeded; UNKNOWN counts (fail closed). Verified against both real runs. | ADAPTED 2026-09-05 |
| IN-18 | No `permissions:` scope lets `GITHUB_TOKEN` read a repository variable (`403 Resource not accessible by integration`), and the `vars` context is a queue-time snapshot (measured, VERIFICATION 7.1). The design read the switch with `GITHUB_TOKEN` in every job and the reader hid the 403, so the layer's first live run declined everything while reporting the switch as absent. | `.github/inbound/killswitch.py`, every `inbound-*.yml` | Fixed 2026-09-05: the App holds Variables (read) and every no-model job mints a `switchtok` token before its first read; model and PR-code jobs start from a gate job's read. A human step: the permission added to the App and accepted on the installation. | ADAPTED |
| IN-19 | The ruleset that confines the App (DESIGN D2: `inbound/**` and `inbound-ledger`, "denies it `main`") applied its `update` rule to `main` as well, so every human merge needed an admin bypass and auto-merge could never fire (#611 waited on a green gate until `--admin`). A ruleset cannot single out one actor; its bypass list admits roles, and the App is not a role. `main` is now excluded from the ruleset and stays under branch protection, which stops a push by anyone; a green PR merged by the App via the API is prevented by construction (no headless job runs a merge verb, `tests/test_inbound_workflows.py` and the deny list forbid the verbs), not by a rule. Found 2026-09-05 at the first merge after the ruleset. | repository ruleset 22311729; `docs/inbound/DESIGN.md` D2; `docs/cicd/RUNBOOK.md` section 9 | Recorded; D2 and RUNBOOK 9 say what the ruleset does and does not deny. A second ruleset on `main` with the roles in `pull_request` bypass mode might deny the App a merge without a bypass prompt for humans; untested, and the next gate-green PR is the test. | ADAPTED |
| IN-20 | The five workflows that push with the App token (`inbound-sweep`, `inbound-fix`, `competitive-run`, `competitive-feed`, `competitive-post`) set `user.email "inbound@users.noreply.github.com"`. A noreply address resolves to whichever account owns that login, and `gh api users/inbound` is a real account created 2012-02-24 that is not ours, so every ledger commit is attributed to a stranger. Found by the C-17 reviewer (cicd FINDINGS C-17, 2026-09-07) after `harness-bot` and `release-bot` turned out to be the same shape. | the five `git config user.email` lines; `tests/test_workflow_commit_identity.py` allowlists them by site until fixed | the App's own address, `<app-id>+<app-login>[bot]@users.noreply.github.com` (the id from `gh api "users/${APP_LOGIN}[bot]" --jq .id` at run time, or a repository variable set in RUNBOOK 9), never `github-actions[bot]`, since the App's login is the one on the CLA allowlist; then drop the allowlist entry and the test enforces it. ⚠ The ratchet reads the workflow TEXT, so the address must be an inline literal (`<id>+<login>[bot]@users.noreply.github.com`), not `"$SOME_VAR"`, or the test must learn to resolve the variable (C-17 review round 4) | FIXED 2026-09-08: the five lines read `325112034+jcodemunch-inbound[bot]@users.noreply.github.com`, an inline literal (the id from `gh api "users/jcodemunch-inbound[bot]" --jq .id` that day, type Bot); `ALLOWED_UNTIL_FIXED` in `tests/test_workflow_commit_identity.py` is empty, so a sixth made-up address fails the ratchet with nowhere to hide, and the App's address is a parametrized owned form. The id is pinned as a literal because the ratchet reads workflow text; if the App is ever re-created its id changes and the ratchet does not see it (the address would resolve to no account, which GitHub renders as an unlinked commit rather than a stranger's), so RUNBOOK 9's App setup names the re-read |
