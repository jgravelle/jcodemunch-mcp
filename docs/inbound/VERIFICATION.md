# VERIFICATION — every headless job and control, exercised (2026-09-04)

One row per property, with the evidence a reader can re-run. Phase 5's
adversarial rows are appended as each job ships; a row marked PENDING
names the PR that will fill it. Run links are added when the run exists on
GitHub; a local run cites its command and its last line.

## 1. Plumbing (DESIGN section 11, item 1)

| property | how exercised | result | evidence |
|---|---|---|---|
| 1.1 kill switch fails closed | `killswitch.enabled()` over `None`, `""`, `True`, `TRUE`, `1`, `yes`, `on`, ` true`, `true\n`; a `gh` failure; a missing variable writing a `skipped` record | every non-`"true"` value is OFF; the record carries `outcome: skipped` and the raw state | `tests/test_inbound_plumbing.py` (kill switch block); `uv run pytest tests/test_inbound_plumbing.py tests/test_build.py -q -p no:xdist` on the reviewed commit: `31 passed` |
| 1.2 budgets decline before a run | `budget.evaluate` at each ceiling (3rd fix run, 3rd open agent PR, 60 USD, 20th triage, 4th depeval); an unknown job; a manual dispatch; an over-ceiling run counted double in `cost_today` | each declines with the reason named; manual lifts nothing | same file, budgets block; the table is asserted against POLICY section 7's text so the two cannot drift silently |
| 1.3 audit record shape | `make_record` yields every POLICY 6.1 field; rejects an unknown field, an unknown outcome, and a security record carrying evidence text | as specified | same file, ledger block |
| 1.4 ledger roll | three artifacts with one duplicate run_id across two months, rolled twice; a record missing a field | 3 appended then 0; two month files; the bad record skipped | same file |
| 1.5 scan reads hidden text | security terms inside an HTML comment, split by zero-width characters, in full-width letters, in `<details>`, as a GHSA id; seven injection phrasings (fake `system:` line, code block, HTML comment, authority claim, post-to-URL, `curl | sh`, "new policy"); a plain bug report | every hidden form found; the plain report trips nothing | `tests/test_inbound_scan.py`; `uv run pytest tests/test_inbound_scan.py tests/test_inbound_prompts.py tests/test_inbound_plumbing.py -q -p no:xdist` on the reviewed commit: `54 passed`. The plumbing PR's reviewer then found eight phrasings inside POLICY 4.3's bullets that passed silently (set a variable, read a secret, `cat ~/.claude`, `--extra-index-url`, disregard, a scheme-less webhook host, `you must`, a fake assistant turn); all eight are red arms in the file now. ⚠ The first draft of the workflow-tamper pattern missed "Disable the pr-gate workflow" because a name sat between verb and noun; widened to allow four words (a ratchet that passed against the reintroduced defect would have hidden this, so the failing case stays in the file) |
| 1.6 prompts are generated, versioned, sha-recorded | `render_prompts.py --check` on the four prompts; a one-byte preamble edit; an edit without a version bump; the same edit with a bump; every workflow-named prompt exists | check clean; the byte edit fails; the unbumped edit is refused by `--write` and flagged by `--check`; the bumped edit renders and VERSIONS.json records version 2 | `tests/test_inbound_prompts.py` |
| 1.7 sdist ships none of it | `.github/` added to the sdist exclude list; asserted by `test_sdist_excludes_the_github_directory`; `tests/test_build.py` still green | as specified | IN-10 |
| 1.8 labels exist | `gh label create --force` for `qa`, `needs-human`, `agent-authored`, `agent-fix`, 13 `inbound:*`, 7 `agent:*` | 24 created, 0 failed: `gh label list --json name` counts 13 `inbound:*`, 7 `agent:*`, plus `agent-authored`, `agent-fix`, `qa`, `needs-human` | IN-1 fixed by the `qa` label |
| 1.9 ledger branch exists | orphan branch `inbound-ledger` with `README.md`, `ledger/`, `drafts/`, `drafts/posted/`, pushed | `55c6b6e` on origin | `git ls-remote --heads origin inbound-ledger` |
| 1.10 App, secrets, variable, ruleset, CLA allowlist, private reporting | human steps (FINDINGS IN-3, IN-4, IN-6, IN-8) | PENDING: nothing in items 2 to 7 runs until these exist; the kill switch is absent, which is OFF | handed over with the plumbing PR |

## 2. Intake and triage (item 2)

Unit and structural half. The end-to-end half (a real issue through intake and the scheduled runner) needs the App and the API key (1.10) and is in section 7.

| property | how exercised | result | evidence |
|---|---|---|---|
| 2.1 no `pull_request_target`, no fork checkout, no persisted credentials | `tests/test_inbound_workflows.py` over every `inbound-*.yml` | both workflows check out `main` with `persist-credentials: false` | 147 passed across the six inbound test files and `test_workflows_pinned.py` on the first item-2 commit; 166 after review round 1 (row 2.8) |
| 2.2 write permissions only on actorless triggers | same file: `contents: write` reserved to the fix job and the sweep; `pull_request` needs the same-repo guard | intake has `issues: write` on `issues`/`issue_comment`; the runner on `schedule`/`workflow_dispatch` | same |
| 2.3 the kill switch precedes every write | same file: the first `killswitch.py` step index is below the first label, comment, `apply_triage`, or model step in every job | holds in both workflows; the runner re-reads the switch before the model and again before `apply_triage` | same |
| 2.4 turns, timeout, model, action pin | same file: `--max-turns 12` at or under POLICY §7, `timeout-minutes` 10 and 5, `claude-sonnet-5` from the prompt's front matter, `anthropics/claude-code-action@ef8bb1e4…` | as specified; `WebFetch`/`WebSearch` disallowed by name; `--permission-mode dontAsk --permission-prompts none` | same |
| 2.5 the plan cannot exceed POLICY §2 | `tests/test_inbound_triage_apply.py`: medium and low never draft or comment; security is label + needs-human at every confidence; duplicate is the one comment and never applies the human `duplicate` label; the owner's issues get labels only; four schema violations refused; a malformed result file (unparseable, the classify step's `{"missing": true}`, absent, or naming a different issue) escalates by WRITING `inbound:unknown` + `needs-human` and removing `inbound:queued` on the issue the workflow passed as `--issue`, while the model's classification never reaches `gh`; every inbound applier takes its write target as a required workflow argument | as specified (corrected 2026-09-13, #670: until then the escalation was planned and never applied, and the test asserted that as the intent) | same |
| 2.6 the scan's false positives on this repo's vocabulary | seven ordinary reports naming env vars, Claude Code hooks, a github.com URL and a pasted `assistant:` log line as green arms; fifteen new red arms from the plumbing reviewer's second round | green arms trip nothing; red arms all found | `tests/test_inbound_scan.py`; the variable/hook pattern now needs a repository qualifier (`repo secret`, `actions variable`, `.claude/hooks`, `deny_guard`) |
| 2.6b credentials fire on exposure, not mention (2026-09-13) | `tests/test_inbound_scan.py`: sentences that only name a credential (#670's own, `GITHUB_TOKEN=""`, "tokens saved", "session token count", "the token cannot leak", "cannot contain secrets", "add an option to not log the api key", "the UI shows the API key field") are green; exposures are red: token, key and PEM/PGP values built at runtime, `Bearer` values, short `password=` values, `*_access_key =` assignments, "not only leaked", "the api key wasn't redacted and is in the log", "my api key (the prod one) was leaked", "the crash report shows the user's real api key", "pasted my token", "the token is logged", "in plain text" inside an HTML comment, a zero-width-split "credential leak"; the credential phrasings of #444/#448/#508 still fire; twenty adversarial inputs of 100,000+ characters each scan in under 3 s | over all 321 issues the word list flagged 75 and the exposure rule flags 20; #444, #448, #508 and #509 are in both; the 55 dropped issues were read in full and none describes an exposure (#407 argues a token cannot leak); an unbounded-affix variant took 205.53 s on 6,000 characters | the corpus and timing runs are pasted in the PR that made the change |
| 2.8 review round 1 (item 2) | the reviewer found: the model step held the App token and `Bash(gh api …/comments:*)` admits a POST form; the apply step's kill-switch re-read ran on the App token, which has no Variables scope, so the runner would have failed closed on every item; intake re-queued held items on any comment; a dispatch input reached `run:` by interpolation | the runner is two jobs, `classify` (read-only, `GITHUB_TOKEN`, no `gh api`) and `apply` (App token, no model); the re-read uses `GITHUB_TOKEN`; `intake_plan.py` never touches a held or classified item and ignores non-author edits (`tests/test_inbound_intake_plan.py`); the input passes through `env` and a digits-only check; two new workflow tests assert no write scope in a model job and no `github.event.*` inside `run:` | 166 passed across the seven inbound files, the pin test and the retirement ledger; ARCHAEOLOGY rows added for the six inbound test files |
| 2.7 the action's inputs | verified against `docs/usage.md` on 2026-09-04: there is no `prompt_file` input; the runner renders the prompt file into a step output and passes `prompt:`; `structured_output` exists when `--json-schema` is passed, but the runner reads the JSON the model writes to `$RUNNER_TEMP` under a path-restricted `Write` rule, so the schema is enforced by `apply_triage.py` and a malformed file escalates | design as built | FINDINGS IN-13 |

## 3. Sweep and digest (items 3, 7)

Sweep (item 3) and digest (item 7), unit and structural half.

| property | how exercised | result | evidence |
|---|---|---|---|
| 3.1 only a literal `approved: true` posts | `decide()` over `None`, `True`, `yes`, `1`, `approved` | every one holds | `tests/test_inbound_sweep.py` |
| 3.2 an App-authored approval never posts | `approver_is_human` against a real `git init` repo: an App-authored commit holds, a later human commit posts | as specified | same file |
| 3.3 an edited body posts but resets the streak; security never graduates | `decide()` with body ≠ original; `update_streaks` two unedited then one edited; a `security` post | edited=True; count 2 → 0 with the reason recorded; `{}` for security | same file |
| 3.4 never posts twice | `post_approved` twice over one approved and one unapproved draft with `gh` mocked | one post, the file moved to `drafts/posted/`, the second sweep holds everything | same file |
| 3.5 the persisted checkout is the ledger branch only | `tests/test_inbound_workflows.py`: a checkout that persists credentials must be `inbound-sweep`, `ref: inbound-ledger`, into a `path`, with the App token; every other checkout is `main` without credentials | the sweep passes on that exception and nothing else does | 64 passed across the sweep, workflow and plumbing tests |
| 3.6 the sweep has no model and writes only the ledger branch | `inbound-sweep.yml`: no `claude-code-action`; `contents: write` is allowed by the workflow test for the fix job only; the sweep's `GITHUB_TOKEN` is read-only and its push is the App's (item-3 review round 2, note 1: the test had still admitted the sweep); the ruleset confining the App to `inbound/**` and `inbound-ledger` is a human step (1.10) | as specified | same |
| 3.7 review round 1 (item 3) | the reviewer found: the two-day artifact window re-copied a draft a human had approved (reverting `approved: true`) and re-created posted drafts; the artifact filter admitted artifacts a fork PR's own workflow uploads under our names; `GITHUB_TOKEN` held `contents: write` and `issues: write` the job never used; one malformed draft aborted the whole sweep | `ingest_drafts` skips any name already in `drafts/` or `drafts/posted/` (three-step test: ingest, approve, re-ingest, post, re-ingest); the collection filter requires our repository id, `head_branch == main`, and a schedule/dispatch/issue/workflow_run producing event; `GITHUB_TOKEN` is `contents: read`, `actions: read`, every write is the App's; a malformed draft holds itself only | `uv run pytest tests/test_inbound_sweep.py tests/test_inbound_workflows.py tests/test_inbound_plumbing.py -q -p no:xdist`: `67 passed` |
| 3.8 every number is code's; the model's paragraph cannot carry one the JSON lacks | `prose_admissible` over a paragraph with `7` against a JSON holding 5 and 12.5; the render-only path with a paragraph saying `99` | the paragraph is dropped and the render JSON names the digit; `99` is absent from the body | `tests/test_inbound_digest.py` |
| 3.9 an absent cost is counted, never priced at 0 | five rows, one with `cost_usd` 12.5, three model runs with `None`, one skip | `cost_by_day_usd` holds the one figure; `runs_with_no_cost_recorded` is 3; the body says `not recorded` and never `0.00` for an empty day | same file |
| 3.10 a kill-switch flip between consecutive records is seen | three rows true, false, true | two flips, in order | same file |
| 3.11 a malformed ledger line holds itself only | one good row and `{not json` | one record counted, one malformed line disclosed | same file |
| 3.12 only an unapproved draft is listed as awaiting | `approved: false`, `approved: true`, and one under `drafts/posted/` | the first only | same file |
| 3.13 needs-human older than 7 days is the sweep's list, and no list is not an empty list | `summarise` with no sweep summary, with an empty list, with `[41, 57]` | `not recorded`; `none as of the sweep at <time>`; the two issues | same file |
| 3.14 the model job holds no write scope and the paragraph is its only write | `tests/test_inbound_workflows.py` over `inbound-digest.yml`: read-only permissions plus `id-token`, `GITHUB_TOKEN` on the action, allow-list `Read,Write(<one path>)`, kill switch re-read before the model and before the App's write, no `contents: write`, no `issues: write` | passes with the other workflows | same |
| 3.15 review round 1 (item 7) | the reviewer found: `prose_admissible` tested each digit-run as a SUBSTRING of the dumped JSON, so `45` passed via a run id and `3` via a date, and number words were never examined (the test JSON lacked the fields that leak); the apply job's audit step hard-coded `kill_switch_state: true` and recorded an apply-time decline as `failed`; `.workflow_runs[0].id` on an empty list is the string `null` and the artifacts call 404'd behind a `| head` that masked it; the `inbound:digest` label did not exist; the prose-artifact download had no fallback; DESIGN 6's original paragraph still said `issues: write` | whole numeric tokens (numbers, decimals, dates, ISO weeks) against the JSON's scalar values and keys, a URL or timestamp contributing nothing, and number words refused, with the prompt (v3) saying digits only; both re-reads pass `--record` so a decline writes `skipped` with the state read, and the audit step yields to an existing record; `// empty` plus a `null` guard and no pipe; the label created (`gh label list`: 13 `inbound:*` plus `inbound:digest`); the paragraph fetched tolerantly by `gh api` so a cancelled prose job still posts; DESIGN amended | `tests/test_inbound_digest.py` (`test_prose_is_admitted_only_when_its_numbers_are_in_the_json` with the real JSON shape and the six leaks as red arms; `test_scalar_tokens_come_from_values_and_keys_never_from_substrings`) |

## 4. Dependency evaluation (item 4)

Unit and structural half; the end-to-end half (a real Dependabot PR through the gate, the evaluation and the bench) needs the App and the API key (1.10) and is in section 7.

| property | how exercised | result | evidence |
|---|---|---|---|
| 4.1 the kind is decided with no model, from the diff itself | `depkind.classify` over a patch bump, a major crossing, a new package, a removed package, a tree-sitter bump beside a major, a diff touching `src/`, a `[project].version` change, an sdist-exclude change, a `uses:` pin bump, a workflow `permissions:` edit, an unpinned `uses:` tag, a pyproject or workflow change handed over with no diff; `lock_versions` over this repo's real `uv.lock` | patch-or-minor / major / major / major / grammar-or-parser / unknown / unknown / unknown / patch-or-minor / unknown / unknown / unknown; the real lock yields `tree-sitter` and `mcp` | `tests/test_inbound_depkind.py` (review round 1, finding 3: the first draft admitted whole files by name) |
| 4.2 `agent:ready-to-merge` needs every input green | `apply_depeval.plan` over the ready case and eight weaker combinations (major, Floor FAIL, gate failure, missing result, no Floor table, REQUEST CHANGES, grammar, unknown) | ready once; evaluation-failed for the gate and Floor cases; bench-pending for grammar; needs-human-review for the rest; a missing Floor table is UNKNOWN and never ready | `tests/test_inbound_depeval_apply.py` |
| 4.3 the Floor table is read by code from the gate's JOB LOG | `floors_hold` over two real `gh run view --log` lines (a PASS and a FAIL, plus a `delegated` line that is not a verdict), the same with the FAIL flipped, a Markdown table, a missing directory, a file with no rows | `(False, ["suite.full_seconds"])`; `(True, [])`; table read; `(None, [])` twice | same file; FINDINGS IN-14 (round 1, finding 1: the gate never uploads its summaries) |
| 4.4 one sticky comment, outcome labels swapped, the append keeps the comment | `apply` with `_gh` mocked returning one marker comment; `append_table` over an existing comment with reasons and over no comment; `_gh` with a real subprocess mock | the comment is PATCHed, no second comment, three labels removed; the appended body starts with the existing text and ends with the table (round 1, finding 2: the first draft PATCHed marker + table over the reasons); `gh api` calls carry no `-R` | same file |
| 4.5 the model's assessment is a draft, never posted | `render_comment` with an assessment in the result; `write_assessment_draft` for major, patch-or-minor and no assessment | the prose is absent from the comment, which names the draft; a `77-r9.md` in the triage draft format for major only | same file (round 1, finding 6) |
| 4.6 the bench table is per row, never per total | `bench_table.render` over two summary shapes with one id present on one side only | the delta column is filled where both sides exist and `n/a` where one is absent; the footer says an absent measurement is never a zero | `tests/test_inbound_bench_table.py` |
| 4.7 the workflow guards are asserted, not described | `tests/test_inbound_workflows.py`: every `pull_request` job carries the same-repo guard (or a `needs:` gate on one that does); a `labeled` trigger names the label and the branch prefix; a `workflow_run` job names the upstream event and actor or branch; no `run:` checks out a PR ref at the root, every worktree is under the runner temp; the model allow-list names no posting verb and no bare `git *`; a job that executes PR code mints no App token; `apply_depeval` is in the write regex | mutants: the label-name guard removed from the bench, the Dependabot actor removed from depeval: each `1 failed, 65 passed` (round 1, findings 4, 5 and note 7) | same file |
| 4.8 review round 1 (item 4) | the reviewer found: `floors_hold` read summary files the gate never uploads (ready unreachable); `append_table` split a tab-joined jq line and erased the sticky comment; `depkind` admitted whole `pyproject.toml` and workflow files by name; VERIFICATION 4.6 claimed workflow-test coverage the test lacked (four guard removals stayed green); "never at the workspace root" was asserted over `actions/checkout` only and the bench minted the App token in the job that executed the merge ref; the assessment was posted, not drafted; notes: the allow-list test denied `gh api` only, a removed package classified patch-or-minor | each fixed as rows 4.1, 4.3, 4.4, 4.5, 4.7 record; the bench is two jobs (`bench` read-only, `apply` with the App); a removed package is `major`; the depeval prompt is version 2 (reads the log and the bench artifact; the assessment is a draft) | this section; `uv run pytest tests/test_inbound_depkind.py tests/test_inbound_depeval_apply.py tests/test_inbound_bench_table.py tests/test_inbound_workflows.py tests/test_inbound_prompts.py tests/test_workflows_pinned.py -q -p no:xdist`: `154 passed` |

## 5. Self-check (item 5)

Unit and structural half; the end-to-end half (an App-authored PR through the check) needs item 6 and the App (1.10) and is in section 7.

| property | how exercised | result | evidence |
|---|---|---|---|
| 5.1 the never-touch list is read from POLICY 4.4 | `never_touch_patterns` over the real policy; eleven never-touch paths and four ordinary ones | every listed path caught, `pyproject.toml` is not a path pattern (the version pin is checked by diff), ordinary paths pass | `tests/test_inbound_selfcheck.py` |
| 5.2 the failing test precedes every src/ change | `commit_order` over test-first, src-first, a mixed first commit, no test commit | the first accepted; the others each named | same file |
| 5.3 the template is read from DESIGN 7 and order matters | nine headings from the design; swapped, missing last | out of order and `## Audit` missing named | same file |
| 5.4 every clause fails for its own reason | `main --skip-red-run` over a PR by a stranger on `feature/x` touching `.claude/settings.json` and the version pin, no template, no Closes | exit 1; clauses (a) to (f) all present | same file |
| 5.5 the workflow runs with the switch off and can only fail | `tests/test_inbound_workflows.py`: `inbound-selfcheck` is exempt from the kill-switch rule; read-only permissions; same-repo guard in the job `if:`; `pull_request` checkout is `main` | as specified | same file |
| 5.6 the red run's files are the PR's, fixtures included, and only exit 1 is red | `test_files_rewritten_after` over a real repo where the test commit's file is rewritten in the fix commit, and where it is not; `test_commit_is_red_on_main(..., head_ref)` end to end over a repo whose test reads a fixture, with the fixture rewritten in the fix commit; `main --head-ref` wired through; `red_from_returncode` over 0, 1, 2, 4, 5 | the rewritten file is named and clause (b) fails; the rewritten FIXTURE is named through the real worktree run; without a head ref the red run alone passes, which is the hole the ref closes; only 1 is red | `tests/test_inbound_selfcheck.py` (round 2: the wiring and the fixture spelling) |
| 5.8 the `uv run --no-sync` seam, by hand | the reviewer ran a worktree test file through the main checkout's environment on this box (round 1) | the package imported was `C:\MCPs\jcodemunch-mcp\src\jcodemunch_mcp`, `sys.path[0]` the worktree; the suite patches this seam (`_pytest_cmd`), so it is exercised only by hand until the section 7 end-to-end run | reviewer round 1, verified without finding (4) |
| 5.7 review rounds 1 to 3 (item 5) | the reviewer found: nothing bound the red run's test files to the PR head (`assert False` first, the real test in the fix commit, passed (b)); the exit-code rule survived mutation to `!= 0` with no test; notes: the token was job-scoped, the DESIGN 5 table disagreed with its as-built paragraph, a dead `**` branch, the `[project].version` string coupling | `test_files_rewritten_after` (row 5.6); `red_from_returncode` pinned; the token scoped to the data step; the table corrected; the branch removed; the coupling named in a comment. Round 2 found the head-ref wiring unexercised (`if head_ref:` -> `if False:` stayed green) and the identity check limited to `.py` files (a rewritten fixture passed): every touched path is compared now and the red run is exercised end to end (row 5.6). Round 3: APPROVE; its note that a test commit could carry a root `pyproject.toml` into the worktree's pytest run is closed by clause (b) refusing any path outside `tests/` in the test commit | this section |

## 6. Fix attempt and promotion (item 6)

Unit and structural half; the end-to-end half (a real `agent-fix` label through the three jobs, the self-check and promotion) needs the App, the API key and `INBOUND_AUTOFIX` absent (1.10) and is in section 7.

| property | how exercised | result | evidence |
|---|---|---|---|
| 6.1 the pre-flight declines before anything runs | `fix_preflight.decide` over a human label on a clean issue; a bot label with `INBOUND_AUTOFIX` unset, set with an established author, a 10-day author, UNKNOWN age and activity; each blocking label with a human label; a revert merged after the last human label, before it, and with no human label at all | proceeds; declines naming the variable; proceeds; declines on age; declines twice (UNKNOWN blocks); declines on each label; declines / proceeds / declines | `tests/test_inbound_fix_gates.py` |
| 6.2 the publish gate decides from the hand-over alone | `fix_publish.decide` over a clean hand-over; five wrong branch shapes; src before the test commit; a never-touch path in any commit; the version pin; a body missing `## Audit`; a body closing the wrong issue | publishes once; every other case named | same file |
| 6.3 the bundle is read for real | a `git init` repo with `main`, a branch of two commits (tests then src), bundled; then `main` moved on | two commits oldest first with their files, the gate publishes; after `main` moves the bundle is "not on top of main" and no commit is listed | same file |
| 6.4 the model job holds no write scope and cannot push | `tests/test_inbound_workflows.py`: the `fix` job's permissions are read plus `id-token`, its action uses `GITHUB_TOKEN`, no App token in the job; the allow-list names no `git push`, no `gh pr`, no bare `git *`; `git remote set-url --push origin no_push` is a step | as specified | same file |
| 6.5 every write is the App's and follows a kill-switch read | `preflight` labels with the App after the switch; `publish` re-reads the switch before minting the App token and before the push; `promote` reads the switch before `gh pr ready` | as specified | same file |
| 6.6 review round 1 (item 6) | the reviewer found: `\| tee` in the pre-flight step reported tee's exit status, so every decline the module computed was ignored and the model ran (the same shape in the self-check step made that check run's conclusion never a failure); a merge or root commit lists no files under `diff-tree`, so a never-touch path could ride into the branch (reproduced with an orphan commit merged in); the promote job matched the App login as a substring on a PR-controlled branch name; the reviewer verdict was bound to the issue, not the SHA; row 6.4 claimed a `no_push` assertion no test contained | redirect-then-show in both steps, and in the FOUR more sites the ratchet found that the reviewer's list did not name (`depkind`, `apply_depeval`, `intake_plan`, `apply_triage`; standing lesson 08-18: write the ratchet before concluding the reported list is the list; its first draft matched per physical line and stayed green with the pipe back, because the invocation and the pipe sit on different continuation lines); `bundle_commits` refuses a commit with other than one parent and `decide` unions `git diff --name-only base..head` into the never-touch check (`test_a_merge_or_root_commit_is_refused_and_the_range_is_checked`); exact login compare in Python; `head-sha.txt` in the hand-over, compared to `workflow_run.head_sha`; `test_the_fix_model_job_cannot_push_to_origin` asserts the `no_push` step precedes the model | `uv run pytest tests/test_inbound_fix_gates.py tests/test_inbound_workflows.py -q -p no:xdist` |
| 6.7 the pre-flight's decline reaches the workflow | mutation: put the `\| tee` back in `inbound-fix.yml` | `test_no_pipe_hides_a_gate_exit_status` goes red | `tests/test_inbound_workflows.py` |

## 7. Adversarial and end-to-end (Phase 5)

The end-to-end half, one row per live run; the scan rows in 1.5 are the
unit-level half of the injection tests.

| property | how exercised | result | evidence |
|---|---|---|---|
| 7.1 the switch is readable by the job that reads it | first live run, 2026-09-05 01:31 UTC, `INBOUND_ENABLED=true` set seconds before: the sweep (dispatch) and the triage runner (schedule) | BOTH declined: `killswitch.py` on `GITHUB_TOKEN` printed `value: null` and the audit record `kill switch INBOUND_ENABLED=None`; a probe workflow on a throwaway branch then measured the two alternatives: `gh api .../actions/variables/INBOUND_ENABLED` on `GITHUB_TOKEN` (`actions: read`) is `403 Resource not accessible by integration`, and `${{ vars.INBOUND_ENABLED }}` read `true` at step 1 and `true` again 120 s later, after the variable had been set to `false` at 01:37:50 by the reviewing session's own `gh variable set` (the time is that shell's clock, not a run log; queue-time snapshot). Fix: every no-model job mints a `switchtok` App token (Variables read) before its first read; model and PR-code jobs have no read and start only from a gate job's `go`; `depeval` and `bench-full` gain a `gate` job so neither is first; the reader prints the error. Ratchets: `test_every_kill_switch_read_uses_the_app_token` (a switch line without the token goes red under mutation), `test_kill_switch_precedes_every_write` (a job without a read must `needs` a job with one and hold no App token), `test_a_failed_switch_read_names_its_error` | runs 33936406280 (sweep), 33936396068 (triage), 33936730813 (probe); FINDINGS IN-18 |
| 7.2 the daily budget lets a one-a-day job run | the same sweep run: `budget.py` printed `runs_per_day: 1 of 1 used` with `runs_today: 1` | the count included the asking run; `count_other_runs` excludes `GITHUB_RUN_ID` (`test_the_run_that_asks_is_not_counted_against_its_own_daily_budget`); a run that declined at its gate still counted until 7.4 (FINDINGS IN-17) | run 33936406280 |
| 7.3 the App token reads the switch | second live sweep, 2026-09-05 02:27:59 UTC on 42c608e (the 7.1 fix merged), switch `true` | the `switchtok` token minted; the read printed `"error": "failed to get variable INBOUND_ENABLED: HTTP 403: Resource not accessible by integration"`, so the error surface works and the App did not yet hold Variables (read) on the installation; the budget ALSO declined, `runs_per_day: 1 of 1 used`, for the morning's declined run (IN-17, closed below); the ruleset had blocked #611's merge until `--admin` (IN-19) | run 33939144859 |
| 7.4 a run that declined at its gate spends no budget | `run_did_work` over run 33939144859 and 33936406280 (both declined) and the fixture shapes | both False; a run with a post-gate step succeeded True; no steps readable True | `test_a_run_that_declined_at_its_gate_spends_no_budget` |
| 7.5 the App token reads the switch and the sweep does its work | third live sweep, 2026-09-05 on 96a38c9 (#612 merged, Variables read on the installation) | the switch read `"value": "true"`; the budget counted `runs_today: 0` (the two declined runs no longer count); the App token minted; the ledger branch checked out; artifacts collected; `sweep.py` ingested and rolled, then FAILED writing `--summary` into `$RUNNER_TEMP/audit/`, a directory only a decline record creates, so the first sweep to pass its gate was the first with nowhere to write; the ledger commit step was skipped and the branch is unchanged. Fix: the summary's directory is created before the write (the digest's `--markdown` too); `test_the_summary_is_written_into_a_directory_that_does_not_exist_yet` | run 33941021629 |
| 7.6 the ledger push by the App under the ruleset | PENDING: the first sweep after 7.5's fix | | |
