using Xunit;

namespace JCodeMunch.Mcp.Tests;

public class PlaceholderTests
{
    [Fact]
    public void ProjectCompiles()
    {
        // Verifies the test project can reference the main project
        Assert.True(true);
    }
}
